<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>7. 代码导读 on Machine Learning 学习笔记</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/</link><description>Recent content in 7. 代码导读 on Machine Learning 学习笔记</description><generator>Hugo</generator><language>en</language><atom:link href="https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/index.xml" rel="self" type="application/rss+xml"/><item><title>7.1 从仓库入口回到请求主线</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/repo-reading-order/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/repo-reading-order/</guid><description>&lt;h1 id="从仓库入口回到请求主线"&gt;从仓库入口回到请求主线&lt;a class="anchor" href="#%e4%bb%8e%e4%bb%93%e5%ba%93%e5%85%a5%e5%8f%a3%e5%9b%9e%e5%88%b0%e8%af%b7%e6%b1%82%e4%b8%bb%e7%ba%bf"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;如果你第一次进入 SGLang 仓库，最容易犯的错误是直接钻进 &lt;code&gt;python/sglang/srt&lt;/code&gt;。这样当然能看到大量核心实现，但你会很快失去“哪些入口是给用户的，哪些入口是给 runtime 的”这层方位感。更稳的读法，是先看四个文件：&lt;code&gt;README.md&lt;/code&gt;、&lt;code&gt;python/sglang/__init__.py&lt;/code&gt;、&lt;code&gt;python/sglang/lang/api.py&lt;/code&gt; 和 &lt;code&gt;python/sglang/launch_server.py&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;README.md&lt;/code&gt; 的价值不是介绍所有内部细节，而是告诉你项目怎样向外描述自己。当前 README 直接把 “Backend Tutorial” 和 “Frontend Tutorial” 分开列出，这能帮助你先建立“双表面”认知。&lt;code&gt;python/sglang/__init__.py&lt;/code&gt; 则把 public surface 展开成 import 入口：一边是 &lt;code&gt;function&lt;/code&gt;、&lt;code&gt;gen&lt;/code&gt;、&lt;code&gt;system&lt;/code&gt;、&lt;code&gt;user&lt;/code&gt;、&lt;code&gt;assistant&lt;/code&gt; 等语言层 API，另一边是 &lt;code&gt;RuntimeEndpoint&lt;/code&gt;、&lt;code&gt;ServerArgs&lt;/code&gt; 与 &lt;code&gt;Engine&lt;/code&gt; 这类运行时入口。&lt;/p&gt;
&lt;p&gt;接着读 &lt;code&gt;python/sglang/lang/api.py&lt;/code&gt;，你会看到 &lt;code&gt;Runtime(...)&lt;/code&gt; 与 &lt;code&gt;Engine(...)&lt;/code&gt; 都只是懒导入包装，而 &lt;code&gt;gen(...)&lt;/code&gt; 则把 &lt;code&gt;regex&lt;/code&gt;、&lt;code&gt;json_schema&lt;/code&gt;、sampling 参数等语言层能力显式暴露出来。最后再看 &lt;code&gt;python/sglang/launch_server.py&lt;/code&gt;，你就能把“language API”“offline engine”“server entrypoint”三类入口区分开，而不是把它们混成一个抽象的“调用 SGLang”。&lt;/p&gt;
&lt;h2 id="为什么按这条顺序读仓库"&gt;为什么按这条顺序读仓库&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e6%8c%89%e8%bf%99%e6%9d%a1%e9%a1%ba%e5%ba%8f%e8%af%bb%e4%bb%93%e5%ba%93"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这样安排顺序，不是为了显得系统化，而是为了减少误读。仓库顶层目录天然混合了“对外表面”“核心 runtime”“示例与测试”“性能与扩展”四类材料。如果你没有先用几个稳定入口把外层地图建立起来，后面看到 &lt;code&gt;entrypoints&lt;/code&gt;、&lt;code&gt;managers&lt;/code&gt;、&lt;code&gt;mem_cache&lt;/code&gt;、&lt;code&gt;benchmark&lt;/code&gt;、&lt;code&gt;sgl-kernel&lt;/code&gt; 时，就很难判断它们在整体叙事里分别扮演什么角色。&lt;/p&gt;
&lt;p&gt;因此，这一章的真正设计理由，是先建立阅读顺序，再建立目录认识。你不是在背一个文件系统，而是在学习一套“从公开入口走回主链路，再从主链路走向扩展材料”的路径。只有这样，代码导读章节才和前面的 request lifecycle、runtime architecture、scheduling/memory 构成同一本书，而不是一组平行笔记。&lt;/p&gt;
&lt;h2 id="回到-pythonsglangsrt-时先找五个目录"&gt;回到 &lt;code&gt;python/sglang/srt&lt;/code&gt; 时，先找五个目录&lt;a class="anchor" href="#%e5%9b%9e%e5%88%b0-pythonsglangsrt-%e6%97%b6%e5%85%88%e6%89%be%e4%ba%94%e4%b8%aa%e7%9b%ae%e5%bd%95"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;带着入口感再回到 &lt;code&gt;python/sglang/srt&lt;/code&gt;，阅读顺序会清楚很多。第一站看 &lt;code&gt;entrypoints/&lt;/code&gt;，因为这里定义了 HTTP、gRPC、engine 等真正进入 runtime 的门。第二站看 &lt;code&gt;managers/&lt;/code&gt;，因为请求状态、队列、批次、流式输出和大量运行时编排都在这里。第三站看 &lt;code&gt;model_executor/&lt;/code&gt;，这里更接近模型前向与设备执行。第四站看 &lt;code&gt;mem_cache/&lt;/code&gt;，这里解释请求状态如何映射到可复用的 KV 存储。第五站看 &lt;code&gt;observability/&lt;/code&gt;，这里提供 metrics、trace 与 request stage 观测能力。&lt;/p&gt;</description></item><item><title>7.2 文档、示例、测试与 benchmark 怎样支撑阅读</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/tests-benchmarks-and-docs/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/tests-benchmarks-and-docs/</guid><description>&lt;h1 id="文档示例测试与-benchmark-怎样支撑阅读"&gt;文档、示例、测试与 benchmark 怎样支撑阅读&lt;a class="anchor" href="#%e6%96%87%e6%a1%a3%e7%a4%ba%e4%be%8b%e6%b5%8b%e8%af%95%e4%b8%8e-benchmark-%e6%80%8e%e6%a0%b7%e6%94%af%e6%92%91%e9%98%85%e8%af%bb"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;代码导读如果只讲源码目录，仍然不够厚。因为真正高质量的技术书，不会把读者只带到实现文件前就停下，而会继续告诉读者：哪些文档是在定义公开表面，哪些示例是在暴露最小可运行路径，哪些测试是在证明维护者认为什么行为不能回归。&lt;/p&gt;
&lt;p&gt;这一章专门补的，就是“源码之外的阅读支撑层”。没有这一层，读者当然知道 &lt;code&gt;python/sglang/srt&lt;/code&gt; 里有哪些模块，但还不知道哪些 examples 值得先跑、哪些 docs 值得回看、哪些 tests 能帮你验证自己对系统的理解。&lt;/p&gt;
&lt;h2 id="docs-不只是说明书而是公开语义层"&gt;&lt;code&gt;docs/&lt;/code&gt; 不只是说明书，而是公开语义层&lt;a class="anchor" href="#docs-%e4%b8%8d%e5%8f%aa%e6%98%af%e8%af%b4%e6%98%8e%e4%b9%a6%e8%80%8c%e6%98%af%e5%85%ac%e5%bc%80%e8%af%ad%e4%b9%89%e5%b1%82"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;官方 &lt;code&gt;docs/basic_usage/&lt;/code&gt;、&lt;code&gt;docs/advanced_features/&lt;/code&gt;、&lt;code&gt;docs/references/&lt;/code&gt; 共同承担了一件很重要的事情：它们把 runtime 和 API 的公共语义稳定下来。你可以把这些文档看成系统的“外部语义层”，它们不是实现本身，但会强烈影响你怎样理解实现。&lt;/p&gt;
&lt;p&gt;例如 OpenAI-compatible、native API、offline engine、structured outputs、tool parser、observability 这些能力，都在 docs 中有比源码更清晰的对外描述。很多时候，先用 docs 确认“系统承诺了什么”，再回源码确认“它如何做到”，比直接读实现更稳。&lt;/p&gt;
&lt;h2 id="examples-告诉你系统最小怎样用起来"&gt;&lt;code&gt;examples/&lt;/code&gt; 告诉你系统最小怎样用起来&lt;a class="anchor" href="#examples-%e5%91%8a%e8%af%89%e4%bd%a0%e7%b3%bb%e7%bb%9f%e6%9c%80%e5%b0%8f%e6%80%8e%e6%a0%b7%e7%94%a8%e8%b5%b7%e6%9d%a5"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;examples/runtime/README.md&lt;/code&gt;、&lt;code&gt;examples/runtime/engine/readme.md&lt;/code&gt;、&lt;code&gt;examples/monitoring/README.md&lt;/code&gt; 这类材料之所以重要，是因为它们把系统能力压成了“最小工作流”。对读者来说，这种材料的价值并不亚于源码，因为它回答了“如果我真的要跑起来，第一步应该怎样做”。&lt;/p&gt;
&lt;p&gt;优秀技术书很少会忽视这一层。因为很多系统真正难懂的地方，不是类和函数，而是“这些东西组合起来之后，最小可运行路径是什么”。examples 恰好就是把这种组合压给读者看的地方。&lt;/p&gt;
&lt;h2 id="test-和-benchmark-让阅读有了验证路径"&gt;&lt;code&gt;test/&lt;/code&gt; 和 &lt;code&gt;benchmark/&lt;/code&gt; 让阅读有了验证路径&lt;a class="anchor" href="#test-%e5%92%8c-benchmark-%e8%ae%a9%e9%98%85%e8%af%bb%e6%9c%89%e4%ba%86%e9%aa%8c%e8%af%81%e8%b7%af%e5%be%84"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;test/README.md&lt;/code&gt; 明确说明了 &lt;code&gt;registered/&lt;/code&gt;、&lt;code&gt;manual/&lt;/code&gt;、&lt;code&gt;run_suite.py&lt;/code&gt;、suite 选择和 CI 约束，这实际上是在告诉读者：维护者如何定义“这次改动算没算破坏系统”。一旦你把测试材料纳入阅读路径，代码导读就从“知道代码在哪”升级成“知道如何证明代码还成立”。&lt;/p&gt;
&lt;p&gt;同样，&lt;code&gt;benchmark/&lt;/code&gt; 也不只是辅助脚本目录。它告诉你系统怎样被衡量、哪些指标被看重、哪些工作流会被重复验证。只是它更适合放在主线之后再读，因为如果你还没理解 request lifecycle、scheduler 和 execution model，就很难真正看懂 benchmark 在测什么。&lt;/p&gt;
&lt;h2 id="一个更完整的阅读闭环"&gt;一个更完整的阅读闭环&lt;a class="anchor" href="#%e4%b8%80%e4%b8%aa%e6%9b%b4%e5%ae%8c%e6%95%b4%e7%9a%84%e9%98%85%e8%af%bb%e9%97%ad%e7%8e%af"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;公开入口（README / docs）
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; -&amp;gt; 主线源码（entrypoints / managers / executor / cache）
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; -&amp;gt; 最小工作流（examples）
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; -&amp;gt; 维护验证（test / benchmark）&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;这条闭环的价值，在于它让“阅读代码”不再是孤立行为。你不是只在看实现，而是在同时看系统怎样被介绍、怎样被运行、怎样被验证。&lt;/p&gt;</description></item><item><title>7.3 按目标阅读仓库：查性能、查接口、查回归该走哪条路</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-by-goal/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-by-goal/</guid><description>&lt;h1 id="按目标阅读仓库查性能查接口查回归该走哪条路"&gt;按目标阅读仓库：查性能、查接口、查回归该走哪条路&lt;a class="anchor" href="#%e6%8c%89%e7%9b%ae%e6%a0%87%e9%98%85%e8%af%bb%e4%bb%93%e5%ba%93%e6%9f%a5%e6%80%a7%e8%83%bd%e6%9f%a5%e6%8e%a5%e5%8f%a3%e6%9f%a5%e5%9b%9e%e5%bd%92%e8%af%a5%e8%b5%b0%e5%93%aa%e6%9d%a1%e8%b7%af"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;很多读者在知道“仓库有哪些目录”之后，还是会卡住，因为真正的工程问题不是“仓库长什么样”，而是“我现在要查什么，该从哪里开始”。这一章就是把仓库阅读从静态地图进一步推进成任务地图：面对不同目标，应该走哪条阅读路径。&lt;/p&gt;
&lt;p&gt;这类内容很像优秀技术书里的“第二层导航”。它不是再讲一遍目录，而是告诉你带着不同问题进入仓库时，最短且最稳的路径是什么。&lt;/p&gt;
&lt;h2 id="如果你的目标是查请求主线"&gt;如果你的目标是查请求主线&lt;a class="anchor" href="#%e5%a6%82%e6%9e%9c%e4%bd%a0%e7%9a%84%e7%9b%ae%e6%a0%87%e6%98%af%e6%9f%a5%e8%af%b7%e6%b1%82%e4%b8%bb%e7%ba%bf"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;当问题是“请求到底怎么走”时，最稳的入口依然是 &lt;code&gt;README.md&lt;/code&gt;、&lt;code&gt;python/sglang/launch_server.py&lt;/code&gt;、&lt;code&gt;python/sglang/srt/entrypoints/http_server.py&lt;/code&gt;、&lt;code&gt;python/sglang/srt/managers/tokenizer_manager.py&lt;/code&gt;、&lt;code&gt;python/sglang/srt/managers/scheduler.py&lt;/code&gt; 和 &lt;code&gt;python/sglang/srt/managers/detokenizer_manager.py&lt;/code&gt;。这条线的价值，是它直接覆盖了“入口、编排、回包”三段主链。&lt;/p&gt;
&lt;p&gt;如果你跳过入口直接去看 &lt;code&gt;scheduler.py&lt;/code&gt;，当然也能看到很多重要逻辑，但更容易失去调用方视角。所以查主链时，先把入口和回程链走通，再去补中间实现，通常是更稳的顺序。&lt;/p&gt;
&lt;h2 id="如果你的目标是查性能和吞吐"&gt;如果你的目标是查性能和吞吐&lt;a class="anchor" href="#%e5%a6%82%e6%9e%9c%e4%bd%a0%e7%9a%84%e7%9b%ae%e6%a0%87%e6%98%af%e6%9f%a5%e6%80%a7%e8%83%bd%e5%92%8c%e5%90%9e%e5%90%90"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这类问题最容易把人引到 benchmark 脚本，但真正稳的读法是先补三层：&lt;code&gt;scheduler.py&lt;/code&gt;、&lt;code&gt;schedule_batch.py&lt;/code&gt;、&lt;code&gt;mem_cache/*&lt;/code&gt;。因为很多性能现象并不是 benchmark 逻辑本身造成的，而是 batch 形成、cache reuse 和 execution path 的结果。&lt;/p&gt;
&lt;p&gt;在这之后，再去看 &lt;code&gt;docs/advanced_features/attention_backend.md&lt;/code&gt;、&lt;code&gt;python/sglang/srt/model_executor/model_runner.py&lt;/code&gt;、&lt;code&gt;bench_one_batch.py&lt;/code&gt;、&lt;code&gt;bench_serving.py&lt;/code&gt; 和 &lt;code&gt;benchmark/&lt;/code&gt; 相关路径，读者就更容易把“指标表现”重新落回具体机制，而不是只看曲线和数字。&lt;/p&gt;
&lt;h2 id="如果你的目标是查接口与协议行为"&gt;如果你的目标是查接口与协议行为&lt;a class="anchor" href="#%e5%a6%82%e6%9e%9c%e4%bd%a0%e7%9a%84%e7%9b%ae%e6%a0%87%e6%98%af%e6%9f%a5%e6%8e%a5%e5%8f%a3%e4%b8%8e%e5%8d%8f%e8%ae%ae%e8%a1%8c%e4%b8%ba"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;当问题是“为什么 OpenAI-compatible / native / offline engine 行为不一样”时，应该先回到 &lt;code&gt;docs/basic_usage/openai_api.rst&lt;/code&gt;、&lt;code&gt;docs/basic_usage/native_api.ipynb&lt;/code&gt;、&lt;code&gt;docs/basic_usage/offline_engine_api.ipynb&lt;/code&gt;，再去看 &lt;code&gt;python/sglang/launch_server.py&lt;/code&gt;、&lt;code&gt;python/sglang/srt/entrypoints/http_server.py&lt;/code&gt; 与 &lt;code&gt;python/sglang/lang/api.py&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;这条路径的重要性在于：协议问题首先是表面问题，其次才是 runtime 问题。优秀技术书通常会把这种“先看表面承诺，再看内部落实”的顺序写清楚，而不是一开始就把读者推进实现深处。&lt;/p&gt;
&lt;h2 id="如果你的目标是查结构化生成与工具调用"&gt;如果你的目标是查结构化生成与工具调用&lt;a class="anchor" href="#%e5%a6%82%e6%9e%9c%e4%bd%a0%e7%9a%84%e7%9b%ae%e6%a0%87%e6%98%af%e6%9f%a5%e7%bb%93%e6%9e%84%e5%8c%96%e7%94%9f%e6%88%90%e4%b8%8e%e5%b7%a5%e5%85%b7%e8%b0%83%e7%94%a8"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这类问题最稳的入口不是 &lt;code&gt;tool parser&lt;/code&gt; 实现本身，而是 &lt;code&gt;docs/advanced_features/structured_outputs.ipynb&lt;/code&gt;、&lt;code&gt;docs/advanced_features/tool_parser.ipynb&lt;/code&gt;、&lt;code&gt;python/sglang/srt/sampling/sampling_params.py&lt;/code&gt;、&lt;code&gt;python/sglang/lang/api.py&lt;/code&gt;。这样读的好处，是你先知道系统承诺了什么，再去看参数对象和 parser 怎样落实这些承诺。&lt;/p&gt;
&lt;p&gt;如果读者直接从 parser 类开始看，往往会很快陷进格式差异和模型特例，而失去“它究竟为什么属于 generation path”的整体理解。&lt;/p&gt;
&lt;h2 id="如果你的目标是查回归与维护路径"&gt;如果你的目标是查回归与维护路径&lt;a class="anchor" href="#%e5%a6%82%e6%9e%9c%e4%bd%a0%e7%9a%84%e7%9b%ae%e6%a0%87%e6%98%af%e6%9f%a5%e5%9b%9e%e5%bd%92%e4%b8%8e%e7%bb%b4%e6%8a%a4%e8%b7%af%e5%be%84"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这时最该先看的不是 &lt;code&gt;python/sglang/srt&lt;/code&gt;，而是 &lt;code&gt;test/README.md&lt;/code&gt;、&lt;code&gt;test/run_suite.py&lt;/code&gt;、&lt;code&gt;test/registered/&lt;/code&gt;、&lt;code&gt;test/manual/&lt;/code&gt; 以及 &lt;code&gt;docs/developer_guide/contribution_guide.md&lt;/code&gt;。因为当问题已经变成“这个改动能不能安全进主线”，第一层信息就不是实现细节，而是验证和提交路径。&lt;/p&gt;
&lt;p&gt;也正因为这样，回归与维护路径属于代码导读不可省略的一部分。对大系统来说，验证结构本身就是系统结构的一部分。&lt;/p&gt;
&lt;h2 id="一个按目标选入口的简表"&gt;一个按目标选入口的简表&lt;a class="anchor" href="#%e4%b8%80%e4%b8%aa%e6%8c%89%e7%9b%ae%e6%a0%87%e9%80%89%e5%85%a5%e5%8f%a3%e7%9a%84%e7%ae%80%e8%a1%a8"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;查请求主线：
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;README -&amp;gt; launch_server.py -&amp;gt; http_server.py -&amp;gt; tokenizer_manager.py -&amp;gt; scheduler.py -&amp;gt; detokenizer_manager.py
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;查性能：
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;scheduler.py -&amp;gt; schedule_batch.py -&amp;gt; mem_cache/* -&amp;gt; attention_backend.md -&amp;gt; model_runner.py -&amp;gt; benchmark/*
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;查接口：
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;openai_api.rst / native_api.ipynb / offline_engine_api.ipynb -&amp;gt; http_server.py / lang/api.py
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;查结构化生成：
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;structured_outputs.ipynb / tool_parser.ipynb -&amp;gt; sampling_params.py -&amp;gt; lang/api.py -&amp;gt; function_call/*
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;查回归与维护：
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;test/README.md -&amp;gt; run_suite.py -&amp;gt; registered/manual tests -&amp;gt; contribution_guide.md&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;h2 id="本章对应哪些代码路径"&gt;本章对应哪些代码路径&lt;a class="anchor" href="#%e6%9c%ac%e7%ab%a0%e5%af%b9%e5%ba%94%e5%93%aa%e4%ba%9b%e4%bb%a3%e7%a0%81%e8%b7%af%e5%be%84"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这一章本质上是把前一章列出的入口重新按任务重组，所以锚点仍然主要落在 &lt;code&gt;README.md&lt;/code&gt;、&lt;code&gt;python/sglang/launch_server.py&lt;/code&gt;、&lt;code&gt;python/sglang/srt/entrypoints/http_server.py&lt;/code&gt;、&lt;code&gt;python/sglang/srt/managers/*&lt;/code&gt;、&lt;code&gt;python/sglang/srt/model_executor/*&lt;/code&gt;、&lt;code&gt;python/sglang/srt/mem_cache/*&lt;/code&gt;、&lt;code&gt;docs/basic_usage/*&lt;/code&gt;、&lt;code&gt;docs/advanced_features/*&lt;/code&gt;、&lt;code&gt;test/README.md&lt;/code&gt; 与 &lt;code&gt;test/run_suite.py&lt;/code&gt;。&lt;/p&gt;</description></item><item><title>7.4 遇到 bug 时，先从仓库哪几处下手</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/where-to-look-for-a-bug/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/where-to-look-for-a-bug/</guid><description>&lt;h1 id="遇到-bug-时先从仓库哪几处下手"&gt;遇到 bug 时，先从仓库哪几处下手&lt;a class="anchor" href="#%e9%81%87%e5%88%b0-bug-%e6%97%b6%e5%85%88%e4%bb%8e%e4%bb%93%e5%ba%93%e5%93%aa%e5%87%a0%e5%a4%84%e4%b8%8b%e6%89%8b"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;知道仓库结构，和真正会用仓库排障，是两件不同的事。很多读者在读完代码导读之后，仍然会在真实问题面前卡住：目录都知道了，但第一步到底该打开哪个文件，第二步该看哪个测试，第三步又该去对照哪份文档？&lt;/p&gt;
&lt;p&gt;这一章就是把“仓库地图”进一步压成“故障入口地图”。它不是再讲一遍目录，而是回答：遇到不同类型的 bug，先去哪几处最可能拿到高价值线索。&lt;/p&gt;
&lt;h2 id="如果-bug-表现为请求进来了但行为不对"&gt;如果 bug 表现为“请求进来了但行为不对”&lt;a class="anchor" href="#%e5%a6%82%e6%9e%9c-bug-%e8%a1%a8%e7%8e%b0%e4%b8%ba%e8%af%b7%e6%b1%82%e8%bf%9b%e6%9d%a5%e4%ba%86%e4%bd%86%e8%a1%8c%e4%b8%ba%e4%b8%8d%e5%af%b9"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这类问题最稳的起手式通常还是主链路：&lt;code&gt;launch_server.py&lt;/code&gt;、&lt;code&gt;http_server.py&lt;/code&gt;、&lt;code&gt;TokenizerManager&lt;/code&gt;、&lt;code&gt;Scheduler&lt;/code&gt;、&lt;code&gt;DetokenizerManager&lt;/code&gt;。因为只要你还没有确认请求究竟卡在 lifecycle 的哪一段，过早跳进 cache 或 backend 往往只会增加噪音。&lt;/p&gt;
&lt;p&gt;一个简单判断是：如果你仍然说不清“请求有没有真正完成闭环”，那就还没到应该读 &lt;code&gt;model_executor&lt;/code&gt; 深处的时候。&lt;/p&gt;
&lt;h2 id="如果-bug-表现为性能或资源行为异常"&gt;如果 bug 表现为“性能或资源行为异常”&lt;a class="anchor" href="#%e5%a6%82%e6%9e%9c-bug-%e8%a1%a8%e7%8e%b0%e4%b8%ba%e6%80%a7%e8%83%bd%e6%88%96%e8%b5%84%e6%ba%90%e8%a1%8c%e4%b8%ba%e5%bc%82%e5%b8%b8"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这类问题最常见的错误入口，是直接去看 benchmark 脚本。更稳的顺序通常是：先回到 &lt;code&gt;scheduler.py&lt;/code&gt;、&lt;code&gt;schedule_batch.py&lt;/code&gt;、&lt;code&gt;memory_pool.py&lt;/code&gt;、&lt;code&gt;radix_cache.py&lt;/code&gt;、&lt;code&gt;chunk_cache.py&lt;/code&gt; 和 &lt;code&gt;model_runner.py&lt;/code&gt;，确认问题更像 batch、cache 还是执行路径差异，然后再去看 benchmark 和 profiling 路径。&lt;/p&gt;
&lt;p&gt;原因很简单：benchmark 往往告诉你“慢了”，但不一定告诉你“为什么慢了”。仓库里最先告诉你原因的，通常还是主机制文件。&lt;/p&gt;
&lt;h2 id="如果-bug-表现为结构化结果或工具调用不稳"&gt;如果 bug 表现为“结构化结果或工具调用不稳”&lt;a class="anchor" href="#%e5%a6%82%e6%9e%9c-bug-%e8%a1%a8%e7%8e%b0%e4%b8%ba%e7%bb%93%e6%9e%84%e5%8c%96%e7%bb%93%e6%9e%9c%e6%88%96%e5%b7%a5%e5%85%b7%e8%b0%83%e7%94%a8%e4%b8%8d%e7%a8%b3"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这时最稳的入口不在 scheduler，而在 &lt;code&gt;SamplingParams&lt;/code&gt;、frontend &lt;code&gt;gen(...)&lt;/code&gt;、&lt;code&gt;tool_parser&lt;/code&gt; 文档与 &lt;code&gt;function_call&lt;/code&gt; 路径。因为这类问题首先是“约束和解释是否一致”，其次才是 runtime 细节。&lt;/p&gt;
&lt;p&gt;也就是说，bug 类型一旦变化，最短阅读路径就应该立刻变化。代码导读真正想提供的，正是这种“不要总从同一个地方开始”的判断能力。&lt;/p&gt;
&lt;h2 id="如果-bug-表现为改动后不确定是否伤到别处"&gt;如果 bug 表现为“改动后不确定是否伤到别处”&lt;a class="anchor" href="#%e5%a6%82%e6%9e%9c-bug-%e8%a1%a8%e7%8e%b0%e4%b8%ba%e6%94%b9%e5%8a%a8%e5%90%8e%e4%b8%8d%e7%a1%ae%e5%ae%9a%e6%98%af%e5%90%a6%e4%bc%a4%e5%88%b0%e5%88%ab%e5%a4%84"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这类问题不属于单个源码文件，而属于“验证路径是否找对”。起手式应当是 &lt;code&gt;test/README.md&lt;/code&gt;、&lt;code&gt;run_suite.py&lt;/code&gt;、对应 &lt;code&gt;registered / unit / manual&lt;/code&gt; 测试路径，以及必要时的 benchmark / profiling 文档。很多时候，读者真正缺的不是更多源码，而是更清楚的验证地图。&lt;/p&gt;
&lt;p&gt;对一本厚一点的技术书来说，这种章节很重要，因为它把“仓库导读”从静态说明，推进成了维护者真正能用的排障工具。&lt;/p&gt;
&lt;h2 id="一个按故障类型选入口的表"&gt;一个按故障类型选入口的表&lt;a class="anchor" href="#%e4%b8%80%e4%b8%aa%e6%8c%89%e6%95%85%e9%9a%9c%e7%b1%bb%e5%9e%8b%e9%80%89%e5%85%a5%e5%8f%a3%e7%9a%84%e8%a1%a8"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;现象：请求不回包
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;先看：launch_server.py / http_server.py / TokenizerManager / Scheduler / DetokenizerManager
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;现象：吞吐或显存异常
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;先看：scheduler.py / schedule_batch.py / memory_pool.py / radix_cache.py / model_runner.py
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;现象：结构化结果不稳
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;先看：sampling_params.py / lang/api.py / tool_parser 文档 / function_call 路径
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;现象：改动后不确定影响范围
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;先看：test/README.md / run_suite.py / registered / unit / benchmark 文档&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;h2 id="本章对应哪些代码路径"&gt;本章对应哪些代码路径&lt;a class="anchor" href="#%e6%9c%ac%e7%ab%a0%e5%af%b9%e5%ba%94%e5%93%aa%e4%ba%9b%e4%bb%a3%e7%a0%81%e8%b7%af%e5%be%84"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这一章本质上回指整本书前面已经建立好的锚点：&lt;code&gt;python/sglang/launch_server.py&lt;/code&gt;、&lt;code&gt;python/sglang/srt/entrypoints/http_server.py&lt;/code&gt;、&lt;code&gt;python/sglang/srt/managers/*&lt;/code&gt;、&lt;code&gt;python/sglang/srt/model_executor/*&lt;/code&gt;、&lt;code&gt;python/sglang/srt/mem_cache/*&lt;/code&gt;、&lt;code&gt;python/sglang/srt/function_call/*&lt;/code&gt;、&lt;code&gt;test/README.md&lt;/code&gt;、&lt;code&gt;test/run_suite.py&lt;/code&gt;。&lt;/p&gt;</description></item><item><title>7.5 通过 tests 反向理解系统行为</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-tests-to-understand-behavior/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-tests-to-understand-behavior/</guid><description>&lt;h1 id="通过-tests-反向理解系统行为"&gt;通过 tests 反向理解系统行为&lt;a class="anchor" href="#%e9%80%9a%e8%bf%87-tests-%e5%8f%8d%e5%90%91%e7%90%86%e8%a7%a3%e7%b3%bb%e7%bb%9f%e8%a1%8c%e4%b8%ba"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;很多读者读代码时只看实现文件，直到某个行为看不懂时才想起去找测试。这种顺序当然可行，但它经常让人忽略一个重要事实：测试文件不只是验证实现，它们本身也在告诉你维护者认为哪些行为是“必须稳定存在的语义”。&lt;/p&gt;
&lt;p&gt;这一章的目标，就是把 tests 从“最后才顺手看”的材料，提升成代码导读中的正式入口。&lt;/p&gt;
&lt;h2 id="为什么-tests-往往比注释更像系统承诺"&gt;为什么 tests 往往比注释更像“系统承诺”&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88-tests-%e5%be%80%e5%be%80%e6%af%94%e6%b3%a8%e9%87%8a%e6%9b%b4%e5%83%8f%e7%b3%bb%e7%bb%9f%e6%89%bf%e8%af%ba"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;实现文件里的注释会解释作者当时怎么想，但测试更像系统今天仍然承诺什么。尤其对 runtime 来说，哪些行为被写进 &lt;code&gt;registered&lt;/code&gt;、哪些路径有专门 unit test、哪些场景有 function calling 或 observability 的回归测试，这些都在告诉你：维护者认为什么不能轻易变。&lt;/p&gt;
&lt;p&gt;这也是为什么用 tests 反向理解系统是很有价值的阅读方式。你不只是在看“代码如何工作”，还在看“哪些工作方式被长期保护”。&lt;/p&gt;
&lt;h2 id="如果你想理解-scheduler--cache先看哪类测试"&gt;如果你想理解 scheduler / cache，先看哪类测试&lt;a class="anchor" href="#%e5%a6%82%e6%9e%9c%e4%bd%a0%e6%83%b3%e7%90%86%e8%a7%a3-scheduler--cache%e5%85%88%e7%9c%8b%e5%93%aa%e7%b1%bb%e6%b5%8b%e8%af%95"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;当问题落在 &lt;code&gt;scheduler&lt;/code&gt;、&lt;code&gt;cache&lt;/code&gt;、allocator 或 reuse / eviction 时，只看 &lt;code&gt;scheduler.py&lt;/code&gt; 和 &lt;code&gt;mem_cache/*&lt;/code&gt; 往往还不够。更稳的补充材料是 &lt;code&gt;test/manual/test_schedule_policy.py&lt;/code&gt;、&lt;code&gt;test/registered/unit/mem_cache/&lt;/code&gt; 以及 SWA / radix cache 相关测试。它们会把一些实现里分散的假设压成更明确的断言。&lt;/p&gt;
&lt;p&gt;这对读者很有帮助，因为它让你看到：“维护者认为这里最容易坏的地方是什么”。而这类信息，很多时候比继续盯大文件里的条件分支更高价值。&lt;/p&gt;
&lt;h2 id="如果你想理解-function-calling--observability为什么测试更重要"&gt;如果你想理解 function calling / observability，为什么测试更重要&lt;a class="anchor" href="#%e5%a6%82%e6%9e%9c%e4%bd%a0%e6%83%b3%e7%90%86%e8%a7%a3-function-calling--observability%e4%b8%ba%e4%bb%80%e4%b9%88%e6%b5%8b%e8%af%95%e6%9b%b4%e9%87%8d%e8%a6%81"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;test/registered/openai_server/function_call/&lt;/code&gt; 这一类路径和 &lt;code&gt;test/registered/observability/&lt;/code&gt; 里的 tracing tests，实际上都在帮助读者理解“外部可见行为到底应该长成什么样”。这些行为很容易在实现层被多个模块共同影响，因此只看一个 parser 或一个 tracing file 往往不够。&lt;/p&gt;
&lt;p&gt;相反，测试会强迫你从结果视角看问题：最终是否真的出现了 tool_calls、streaming 片段是否能正确拼回去、某个 &lt;code&gt;RequestStage&lt;/code&gt; 是否真的被记录。这种“从结果倒逼理解”的阅读方式，是很多优秀技术书都会鼓励读者掌握的能力。&lt;/p&gt;
&lt;h2 id="一个更像技术书练习的阅读法"&gt;一个更像技术书练习的阅读法&lt;a class="anchor" href="#%e4%b8%80%e4%b8%aa%e6%9b%b4%e5%83%8f%e6%8a%80%e6%9c%af%e4%b9%a6%e7%bb%83%e4%b9%a0%e7%9a%84%e9%98%85%e8%af%bb%e6%b3%95"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;如果你读完实现还不够踏实，可以尝试做这样一个小练习：&lt;/p&gt;</description></item><item><title>7.6 从 README 到 benchmark：一条更完整的仓库主线</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/from-readme-to-benchmarks/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/from-readme-to-benchmarks/</guid><description>&lt;h1 id="从-readme-到-benchmark一条更完整的仓库主线"&gt;从 README 到 benchmark：一条更完整的仓库主线&lt;a class="anchor" href="#%e4%bb%8e-readme-%e5%88%b0-benchmark%e4%b8%80%e6%9d%a1%e6%9b%b4%e5%ae%8c%e6%95%b4%e7%9a%84%e4%bb%93%e5%ba%93%e4%b8%bb%e7%ba%bf"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面几章已经把“按目标读仓库”“通过 tests 读系统行为”“遇到 bug 从哪里下手”这些角度分别讲开了，但对一本更厚的技术书来说，还需要再把这些角度重新串起来：如果一个读者准备从零进入这个仓库，一路走到 benchmark 与 profiling，他的最佳路径到底是什么？&lt;/p&gt;
&lt;p&gt;这一章要解决的，就是“如何把分散的阅读策略重新压成一条更长、更稳的仓库主线”。&lt;/p&gt;
&lt;h2 id="为什么从-readme-开始仍然是对的"&gt;为什么从 README 开始仍然是对的&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e4%bb%8e-readme-%e5%bc%80%e5%a7%8b%e4%bb%8d%e7%84%b6%e6%98%af%e5%af%b9%e7%9a%84"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;很多进阶读者会天然觉得 README 太浅，不值得花太多时间。但对一个复杂系统来说，README 的价值从来不是“够不够深”，而是“能不能帮你在最早阶段避免误判系统形状”。SGLang 尤其如此，因为它同时有 frontend language、server runtime、native API、offline engine、structured output 和 observability 这些外部表面。&lt;/p&gt;
&lt;p&gt;因此，把 README 放在阅读链最前面，并不是迁就新手，而是把系统形状先讲正。只要形状一开始没看偏，后面进入 &lt;code&gt;srt&lt;/code&gt;、tests 和 benchmark 才不至于越读越散。&lt;/p&gt;
&lt;h2 id="为什么阅读路径应该最终落到-benchmark"&gt;为什么阅读路径应该最终落到 benchmark&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e9%98%85%e8%af%bb%e8%b7%af%e5%be%84%e5%ba%94%e8%af%a5%e6%9c%80%e7%bb%88%e8%90%bd%e5%88%b0-benchmark"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;很多代码导读会在“知道源码在哪”之后结束，但大系统真正的终点往往不是源码，而是“怎样证明你的理解和修改在运行上站得住”。这也是为什么这章要把 benchmark 和 profiling 拉进来。&lt;/p&gt;
&lt;p&gt;对 SGLang 这种 inference runtime 而言，benchmark 不是额外附录，而是“理解是否最终落地”的一层证据。你理解了请求主线、scheduler、cache、execution model 之后，最终总要回到 benchmark / profiling 问：这些理解怎样影响真实运行结果。&lt;/p&gt;
&lt;h2 id="一条更完整的阅读路径"&gt;一条更完整的阅读路径&lt;a class="anchor" href="#%e4%b8%80%e6%9d%a1%e6%9b%b4%e5%ae%8c%e6%95%b4%e7%9a%84%e9%98%85%e8%af%bb%e8%b7%af%e5%be%84"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;README
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; -&amp;gt; __init__.py / lang/api.py / launch_server.py
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; -&amp;gt; entrypoints / managers / model_executor / mem_cache / observability
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; -&amp;gt; docs/basic_usage / advanced_features
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; -&amp;gt; examples/runtime / examples/monitoring
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; -&amp;gt; test/README.md / run_suite.py / registered tests
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; -&amp;gt; benchmark_and_profiling / bench_one_batch / bench_serving / benchmark/*&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;这条路径的意义，不是让读者机械照抄，而是说明：一个系统性的技术读法，最终应该从“项目怎样介绍自己”走到“项目怎样证明自己”。这正是技术书比普通源码介绍更有价值的地方。&lt;/p&gt;</description></item><item><title>7.7 从 benchmark 和 profiler 反向理解实现</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-performance-evidence/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-performance-evidence/</guid><description>&lt;h1 id="从-benchmark-和-profiler-反向理解实现"&gt;从 benchmark 和 profiler 反向理解实现&lt;a class="anchor" href="#%e4%bb%8e-benchmark-%e5%92%8c-profiler-%e5%8f%8d%e5%90%91%e7%90%86%e8%a7%a3%e5%ae%9e%e7%8e%b0"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;很多读者在读到 benchmark 或 profiler 输出时，会把它们当成“性能结果展示”。但对一本更厚的技术书来说，性能证据的价值远不止结果本身，它还应该反过来帮助读者更准确地理解 &lt;code&gt;scheduler&lt;/code&gt;、&lt;code&gt;cache&lt;/code&gt;、&lt;code&gt;execution model&lt;/code&gt; 到底在哪里花时间、为什么会花时间。&lt;/p&gt;
&lt;p&gt;这一章的目标，就是把 benchmark / profiler 从“结果页”重新变成“源码阅读入口”。&lt;/p&gt;
&lt;h2 id="为什么性能证据不该停在图表上"&gt;为什么性能证据不该停在图表上&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e6%80%a7%e8%83%bd%e8%af%81%e6%8d%ae%e4%b8%8d%e8%af%a5%e5%81%9c%e5%9c%a8%e5%9b%be%e8%a1%a8%e4%b8%8a"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;如果 benchmark 只是告诉你吞吐高低，它的价值仍然有限。真正高价值的部分在于：这些结果能否帮助你更快回到 &lt;code&gt;scheduler.py&lt;/code&gt;、&lt;code&gt;memory_pool.py&lt;/code&gt;、&lt;code&gt;model_runner.py&lt;/code&gt;、&lt;code&gt;ForwardBatch&lt;/code&gt; 或 graph runner 路径，解释“为什么会得到这样的曲线”。&lt;/p&gt;
&lt;p&gt;这也是为什么好技术书喜欢把性能证据重新接回实现，而不是只展示结论。因为只有这样，读者拿到的才不只是数字，而是更强的判断框架。&lt;/p&gt;
&lt;h2 id="一条更像维护者的反向路径"&gt;一条更像维护者的反向路径&lt;a class="anchor" href="#%e4%b8%80%e6%9d%a1%e6%9b%b4%e5%83%8f%e7%bb%b4%e6%8a%a4%e8%80%85%e7%9a%84%e5%8f%8d%e5%90%91%e8%b7%af%e5%be%84"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;benchmark / profiler 结果
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; -&amp;gt; 判断更像 scheduler / cache / execution / protocol 表面问题
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; -&amp;gt; 回到对应章节和源码锚点
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt; -&amp;gt; 验证假设&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;这条路径的关键是：不要让 profiler 和 benchmark 变成“最后才看的旁支材料”。它们应该反过来推动你重新读核心实现。&lt;/p&gt;
&lt;h2 id="本章对应哪些代码路径"&gt;本章对应哪些代码路径&lt;a class="anchor" href="#%e6%9c%ac%e7%ab%a0%e5%af%b9%e5%ba%94%e5%93%aa%e4%ba%9b%e4%bb%a3%e7%a0%81%e8%b7%af%e5%be%84"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这一章的锚点主要包括 &lt;code&gt;python/sglang/bench_one_batch.py&lt;/code&gt;、&lt;code&gt;python/sglang/bench_serving.py&lt;/code&gt;、&lt;code&gt;python/sglang/profiler.py&lt;/code&gt;、&lt;code&gt;docs/developer_guide/benchmark_and_profiling.md&lt;/code&gt;、&lt;code&gt;docs/developer_guide/bench_serving.md&lt;/code&gt;，以及它们回指的 &lt;code&gt;scheduler.py&lt;/code&gt;、&lt;code&gt;memory_pool.py&lt;/code&gt;、&lt;code&gt;model_runner.py&lt;/code&gt; 等核心实现。&lt;/p&gt;
&lt;h2 id="小结"&gt;小结&lt;a class="anchor" href="#%e5%b0%8f%e7%bb%93"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;性能证据不是附录。把 benchmark 和 profiler 重新接回源码主线之后，代码导读这一大节才更像一本成熟技术书，而不是一份“看完目录就结束”的导航。&lt;/p&gt;</description></item><item><title>7.8 读 entrypoints/openai：协议表面怎样回到 runtime</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-openai-entrypoints/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-openai-entrypoints/</guid><description>&lt;h1 id="读-entrypointsopenai协议表面怎样回到-runtime"&gt;读 entrypoints/openai：协议表面怎样回到 runtime&lt;a class="anchor" href="#%e8%af%bb-entrypointsopenai%e5%8d%8f%e8%ae%ae%e8%a1%a8%e9%9d%a2%e6%80%8e%e6%a0%b7%e5%9b%9e%e5%88%b0-runtime"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;代码导读前面的章节已经把仓库主线、tests、benchmark 和故障入口讲得比较厚了，但还有一层非常值得单独讲：如果你想从协议表面往里读，&lt;code&gt;python/sglang/srt/entrypoints/openai/&lt;/code&gt; 这一整层到底该怎样读？很多工程读者会把这里当成“单纯的接口适配目录”，但它实际上承接了模板解释、请求转换、stream/non-stream 分支、tool call 语义翻译和最终 runtime 调用。&lt;/p&gt;
&lt;p&gt;这一章的目标，就是把 &lt;code&gt;entrypoints/openai&lt;/code&gt; 从“接口目录”提升成一条可读的调用栈。&lt;/p&gt;
&lt;h2 id="为什么这层值得放进代码导读而不是只放在-api-章节"&gt;为什么这层值得放进代码导读，而不是只放在 API 章节&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e5%b1%82%e5%80%bc%e5%be%97%e6%94%be%e8%bf%9b%e4%bb%a3%e7%a0%81%e5%af%bc%e8%af%bb%e8%80%8c%e4%b8%8d%e6%98%af%e5%8f%aa%e6%94%be%e5%9c%a8-api-%e7%ab%a0%e8%8a%82"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;API 章节关注“为什么有这些 surface”；代码导读章节关注“如果我要顺着源码把这个 surface 读进去，应该怎么走”。对 &lt;code&gt;entrypoints/openai&lt;/code&gt; 来说，这两者不是同一件事。前者回答的是协议职责，后者回答的是阅读路径。&lt;/p&gt;
&lt;p&gt;从代码组织看，这层至少同时涉及：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;serving_base.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;serving_chat.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;serving_completions.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;serving_responses.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;protocol.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;http_server.py&lt;/code&gt; 中的路由绑定&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果不把这些路径压成一章，读者很容易在 &lt;code&gt;serving_chat.py&lt;/code&gt; 里迷路，不知道哪些逻辑是共用的，哪些逻辑是 chat 特有的。&lt;/p&gt;
&lt;h2 id="一张图openai-compatible-请求怎样落回-runtime"&gt;一张图：OpenAI-compatible 请求怎样落回 runtime&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%beopenai-compatible-%e8%af%b7%e6%b1%82%e6%80%8e%e6%a0%b7%e8%90%bd%e5%9b%9e-runtime"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：许多读者会直接跳进 &lt;code&gt;serving_chat.py&lt;/code&gt;，结果把“共用基类逻辑”和“chat 特有逻辑”混在一起。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 Route[&amp;#34;http_server route&amp;#34;] --&amp;gt; Base[&amp;#34;OpenAIServingBase.handle_request()&amp;#34;]
 Base --&amp;gt; Convert[&amp;#34;_convert_to_internal_request()&amp;#34;]
 Convert --&amp;gt; Stream[&amp;#34;_handle_streaming_request() / _handle_non_streaming_request()&amp;#34;]
 Stream --&amp;gt; TM[&amp;#34;TokenizerManager.generate_request()&amp;#34;]
 TM --&amp;gt; Runtime[&amp;#34;scheduler / detokenizer / response path&amp;#34;]&lt;/pre&gt;&lt;p&gt;这张图比纯文字多解释的一点是：OpenAI-compatible surface 并不是每个 handler 各写一套逻辑，而是先经过基类统一模式，再落到各自的请求转换和结果翻译。&lt;/p&gt;</description></item><item><title>7.9 读 protocol.py 与 io_struct.py：公开协议怎样变成内部对象</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/protocol-and-io-struct-bridge/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/protocol-and-io-struct-bridge/</guid><description>&lt;h1 id="读-protocolpy-与-io_structpy公开协议怎样变成内部对象"&gt;读 protocol.py 与 io_struct.py：公开协议怎样变成内部对象&lt;a class="anchor" href="#%e8%af%bb-protocolpy-%e4%b8%8e-io_structpy%e5%85%ac%e5%bc%80%e5%8d%8f%e8%ae%ae%e6%80%8e%e6%a0%b7%e5%8f%98%e6%88%90%e5%86%85%e9%83%a8%e5%af%b9%e8%b1%a1"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;代码导读前面已经把 &lt;code&gt;entrypoints/openai&lt;/code&gt; 的 handler 骨架讲出来了，但还有一层非常值得单独补：请求在协议层用的是 Pydantic request/response model，而进入 runtime 之后又会变成 dataclass 风格的内部对象。这条桥如果不讲清楚，读者会很容易在 &lt;code&gt;protocol.py&lt;/code&gt; 和 &lt;code&gt;io_struct.py&lt;/code&gt; 之间反复跳，却始终不确定“这两个文件到底是不是在描述同一条请求”。&lt;/p&gt;
&lt;p&gt;这一章就是把这条桥讲透。&lt;/p&gt;
&lt;h2 id="为什么这层对读仓库特别重要"&gt;为什么这层对读仓库特别重要&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e5%b1%82%e5%af%b9%e8%af%bb%e4%bb%93%e5%ba%93%e7%89%b9%e5%88%ab%e9%87%8d%e8%a6%81"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;如果你只读 &lt;code&gt;protocol.py&lt;/code&gt;，你会以为系统关心的是 OpenAI-compatible request schema；如果你只读 &lt;code&gt;io_struct.py&lt;/code&gt;，你又会以为系统关心的是 manager 之间传递的运行时对象。真正的情况是：这两者分别站在同一条请求的外层和内层。&lt;/p&gt;
&lt;p&gt;这也是为什么这章更适合放在代码导读而不是 API 章节里。它不是解释协议本身，而是解释“协议对象怎样真正走进运行时内部”。&lt;/p&gt;
&lt;h2 id="一张图协议对象与内部对象之间的桥"&gt;一张图：协议对象与内部对象之间的桥&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%be%e5%8d%8f%e8%ae%ae%e5%af%b9%e8%b1%a1%e4%b8%8e%e5%86%85%e9%83%a8%e5%af%b9%e8%b1%a1%e4%b9%8b%e9%97%b4%e7%9a%84%e6%a1%a5"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会把 &lt;code&gt;ChatCompletionRequest&lt;/code&gt; 和 &lt;code&gt;GenerateReqInput&lt;/code&gt; 看成两套平行系统，而不是同一条变换链上的前后状态。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 Proto[&amp;#34;protocol.py Pydantic models&amp;#34;] --&amp;gt; Serve[&amp;#34;serving_*._convert_to_internal_request()&amp;#34;]
 Serve --&amp;gt; IO[&amp;#34;io_struct.py request objects&amp;#34;]
 IO --&amp;gt; Batch[&amp;#34;tokenized / batch output structs&amp;#34;]
 Batch --&amp;gt; Runtime[&amp;#34;scheduler / detokenizer / tokenizer manager&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：&lt;code&gt;protocol.py&lt;/code&gt; 和 &lt;code&gt;io_struct.py&lt;/code&gt; 不是重复定义，而是协议对象与运行时对象的两端。&lt;/p&gt;
&lt;h2 id="protocolpy-更像什么"&gt;&lt;code&gt;protocol.py&lt;/code&gt; 更像什么&lt;a class="anchor" href="#protocolpy-%e6%9b%b4%e5%83%8f%e4%bb%80%e4%b9%88"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;entrypoints/openai/protocol.py&lt;/code&gt; 明确写着自己是 “Pydantic models for OpenAI API protocol”。这里的对象重点在于：&lt;/p&gt;</description></item><item><title>7.10 读 Engine Python API：它与 HTTP server 共享了什么核心</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-engine-and-http-shared-core/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-engine-and-http-shared-core/</guid><description>&lt;h1 id="读-engine-python-api它与-http-server-共享了什么核心"&gt;读 Engine Python API：它与 HTTP server 共享了什么核心&lt;a class="anchor" href="#%e8%af%bb-engine-python-api%e5%ae%83%e4%b8%8e-http-server-%e5%85%b1%e4%ba%ab%e4%ba%86%e4%bb%80%e4%b9%88%e6%a0%b8%e5%bf%83"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;代码导读前面已经把 &lt;code&gt;entrypoints/openai&lt;/code&gt;、&lt;code&gt;protocol.py&lt;/code&gt; 和 &lt;code&gt;io_struct.py&lt;/code&gt; 串起来了，但还有一条非常值得补的阅读路径：如果你不是从 HTTP server 进入，而是从 Python &lt;code&gt;Engine&lt;/code&gt; API 进入，这条路到底和 HTTP shared core 共享了什么，又在哪些地方开始分叉？&lt;/p&gt;
&lt;p&gt;这一章就是把 &lt;code&gt;Engine&lt;/code&gt; 这条阅读路径补进来。&lt;/p&gt;
&lt;h2 id="为什么这层值得单独讲"&gt;为什么这层值得单独讲&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e5%b1%82%e5%80%bc%e5%be%97%e5%8d%95%e7%8b%ac%e8%ae%b2"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;很多读者会天然把 &lt;code&gt;Engine.generate(...)&lt;/code&gt;、&lt;code&gt;Engine.encode(...)&lt;/code&gt; 当成“另一套实现”。从 &lt;code&gt;engine.py&lt;/code&gt; 看，这并不准确。它们在很大程度上共享同一套 &lt;code&gt;TokenizerManager.generate_request(...)&lt;/code&gt; 主路径，只是少了 HTTP 路由、协议 schema 和 surface 级翻译。&lt;/p&gt;
&lt;p&gt;把这层讲出来，对整本书非常重要，因为它能让读者真正理解：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;HTTP server 和 Python API 不只是“都能用 SGLang”&lt;/li&gt;
&lt;li&gt;它们很多时候是在复用同一条 runtime 核心&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="一张图engine-api-与-http-server-并不是两套独立系统"&gt;一张图：Engine API 与 HTTP server 并不是两套独立系统&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%beengine-api-%e4%b8%8e-http-server-%e5%b9%b6%e4%b8%8d%e6%98%af%e4%b8%a4%e5%a5%97%e7%8b%ac%e7%ab%8b%e7%b3%bb%e7%bb%9f"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多人会把 &lt;code&gt;Engine&lt;/code&gt; 和 &lt;code&gt;http_server&lt;/code&gt; 想成两条平行道路，但实际上它们在很深的位置汇合。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 HTTP[&amp;#34;HTTP / OpenAI surface&amp;#34;] --&amp;gt; Route[&amp;#34;serving handler / request conversion&amp;#34;]
 PY[&amp;#34;Engine.generate / encode / rerank&amp;#34;] --&amp;gt; Obj[&amp;#34;GenerateReqInput / EmbeddingReqInput&amp;#34;]
 Route --&amp;gt; Obj
 Obj --&amp;gt; TM[&amp;#34;TokenizerManager.generate_request()&amp;#34;]
 TM --&amp;gt; Runtime[&amp;#34;scheduler / detokenizer / managers&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：区别更多发生在“对象如何被构造”，而不是“对象进入运行时之后怎么跑”。&lt;/p&gt;</description></item><item><title>7.11 读 scheduler.py 与 schedule_batch.py：主循环和对象骨架</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-scheduler-and-schedule-batch/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-scheduler-and-schedule-batch/</guid><description>&lt;h1 id="读-schedulerpy-与-schedule_batchpy主循环和对象骨架"&gt;读 scheduler.py 与 schedule_batch.py：主循环和对象骨架&lt;a class="anchor" href="#%e8%af%bb-schedulerpy-%e4%b8%8e-schedule_batchpy%e4%b8%bb%e5%be%aa%e7%8e%af%e5%92%8c%e5%af%b9%e8%b1%a1%e9%aa%a8%e6%9e%b6"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;代码导读讲到这里，读者已经知道该怎样从入口读到 runtime、从协议对象读到内部对象，但还差一条最硬的阅读路径：如果你真的准备深入 &lt;code&gt;scheduler.py&lt;/code&gt;，该怎样避免被几千行代码和一堆辅助对象淹没？更具体地说，&lt;code&gt;scheduler.py&lt;/code&gt; 和 &lt;code&gt;schedule_batch.py&lt;/code&gt; 应该怎样配对阅读，才能先抓住主循环和对象骨架，再读局部策略和边界条件。&lt;/p&gt;
&lt;p&gt;这一章的目标，就是把这条最硬的源码阅读路径讲清楚。&lt;/p&gt;
&lt;h2 id="为什么这两个文件应该一起读"&gt;为什么这两个文件应该一起读&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e4%b8%a4%e4%b8%aa%e6%96%87%e4%bb%b6%e5%ba%94%e8%af%a5%e4%b8%80%e8%b5%b7%e8%af%bb"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;单独读 &lt;code&gt;scheduler.py&lt;/code&gt;，你会看到大量流程：收请求、排队、选 batch、跑 forward、处理结果、retract、flush、abort、disaggregation event loop。单独读 &lt;code&gt;schedule_batch.py&lt;/code&gt;，你又会看到一堆对象：&lt;code&gt;Req&lt;/code&gt;、&lt;code&gt;ScheduleBatch&lt;/code&gt;、&lt;code&gt;PrefillAdder&lt;/code&gt;、各种 mixin、各种状态字段。真正高效的读法，是把两者配成一组：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;scheduler.py&lt;/code&gt; 讲“流程怎么走”&lt;/li&gt;
&lt;li&gt;&lt;code&gt;schedule_batch.py&lt;/code&gt; 讲“流程在操作哪些对象”&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果你把它们拆开读，很容易只记住很多函数名，记不住系统到底在推进什么状态。&lt;/p&gt;
&lt;h2 id="一张图为什么它们是一对骨架文件"&gt;一张图：为什么它们是一对骨架文件&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%be%e4%b8%ba%e4%bb%80%e4%b9%88%e5%ae%83%e4%bb%ac%e6%98%af%e4%b8%80%e5%af%b9%e9%aa%a8%e6%9e%b6%e6%96%87%e4%bb%b6"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会把 &lt;code&gt;scheduler.py&lt;/code&gt; 当成唯一核心文件，而忽略 &lt;code&gt;schedule_batch.py&lt;/code&gt; 才是它真正反复操作的对象骨架。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 Recv[&amp;#34;recv request&amp;#34;] --&amp;gt; Build[&amp;#34;Req / ScheduleBatch build-up&amp;#34;]
 Build --&amp;gt; Select[&amp;#34;get_next_batch_to_run()&amp;#34;]
 Select --&amp;gt; Run[&amp;#34;forward / process_batch_result&amp;#34;]
 Run --&amp;gt; Update[&amp;#34;running_batch / last_batch update&amp;#34;]
 Update --&amp;gt; Build&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：scheduler 主循环并不是在空中飞，它始终围绕 &lt;code&gt;Req&lt;/code&gt; 和 &lt;code&gt;ScheduleBatch&lt;/code&gt; 两类对象反复推进。&lt;/p&gt;
&lt;h2 id="schedule_batchpy-文件头已经给了最重要的阅读提示"&gt;&lt;code&gt;schedule_batch.py&lt;/code&gt; 文件头已经给了最重要的阅读提示&lt;a class="anchor" href="#schedule_batchpy-%e6%96%87%e4%bb%b6%e5%a4%b4%e5%b7%b2%e7%bb%8f%e7%bb%99%e4%ba%86%e6%9c%80%e9%87%8d%e8%a6%81%e7%9a%84%e9%98%85%e8%af%bb%e6%8f%90%e7%a4%ba"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这个文件一开头就直接写出：&lt;/p&gt;
&lt;p&gt;&lt;code&gt;ScheduleBatch -&amp;gt; ModelWorkerBatch -&amp;gt; ForwardBatch&lt;/code&gt;&lt;/p&gt;</description></item><item><title>7.12 用 bench_one_batch.py 做最小执行显微镜</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/bench-one-batch-as-microscope/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/bench-one-batch-as-microscope/</guid><description>&lt;h1 id="用-bench_one_batchpy-做最小执行显微镜"&gt;用 bench_one_batch.py 做最小执行显微镜&lt;a class="anchor" href="#%e7%94%a8-bench_one_batchpy-%e5%81%9a%e6%9c%80%e5%b0%8f%e6%89%a7%e8%a1%8c%e6%98%be%e5%be%ae%e9%95%9c"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;代码导读前面已经给出了从 README、HTTP surface、Engine API、&lt;code&gt;protocol.py&lt;/code&gt;、&lt;code&gt;io_struct.py&lt;/code&gt; 到 &lt;code&gt;scheduler.py&lt;/code&gt; 的多条阅读路径，但还差一种非常高效的“缩小版执行入口”：如果你不想先启动整个 server，也不想一上来就读完整 runtime 主线，有没有一个更小、更可控的文件能帮你直接看到“准备输入 -&amp;gt; 构造 batch -&amp;gt; forward -&amp;gt; sample -&amp;gt; 计时/profile”的最小闭环？&lt;/p&gt;
&lt;p&gt;&lt;code&gt;python/sglang/bench_one_batch.py&lt;/code&gt; 正是这样一个入口。&lt;/p&gt;
&lt;h2 id="为什么这个文件特别适合拿来当显微镜"&gt;为什么这个文件特别适合拿来当“显微镜”&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e4%b8%aa%e6%96%87%e4%bb%b6%e7%89%b9%e5%88%ab%e9%80%82%e5%90%88%e6%8b%bf%e6%9d%a5%e5%bd%93%e6%98%be%e5%be%ae%e9%95%9c"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;它的文件头就把目标说得很清楚：&lt;code&gt;Benchmark the latency of running a single static batch without a server.&lt;/code&gt; 这句话非常关键，因为它意味着这个脚本故意绕开了完整 server path，专门留下低层执行路径。&lt;/p&gt;
&lt;p&gt;这让它特别适合做两件事：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;作为理解执行模型的缩小版入口&lt;/li&gt;
&lt;li&gt;作为性能/正确性实验时的最小复现入口&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;一本更厚的技术书不应该只让读者在大系统里游泳，也应该给他几个“可以单步观察”的浅水区。&lt;code&gt;bench_one_batch.py&lt;/code&gt; 就是其中之一。&lt;/p&gt;
&lt;h2 id="一张图这个脚本把完整系统压成了哪条最小链路"&gt;一张图：这个脚本把完整系统压成了哪条最小链路&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%be%e8%bf%99%e4%b8%aa%e8%84%9a%e6%9c%ac%e6%8a%8a%e5%ae%8c%e6%95%b4%e7%b3%bb%e7%bb%9f%e5%8e%8b%e6%88%90%e4%ba%86%e5%93%aa%e6%9d%a1%e6%9c%80%e5%b0%8f%e9%93%be%e8%b7%af"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会把 benchmark 脚本想成“只是跑一下性能”，而忽略它实际上已经保留了执行闭环中最核心的几步。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 Input[&amp;#34;prepare prompts / ids&amp;#34;] --&amp;gt; Req[&amp;#34;Req / ScheduleBatch&amp;#34;]
 Req --&amp;gt; Fwd[&amp;#34;ForwardBatch / ModelRunner.forward()&amp;#34;]
 Fwd --&amp;gt; Sample[&amp;#34;model_runner.sample()&amp;#34;]
 Sample --&amp;gt; Measure[&amp;#34;latency / profile / correctness output&amp;#34;]&lt;/pre&gt;&lt;p&gt;这张图比纯文字多解释的一点是：这个脚本不是在跑“另一套逻辑”，而是在把完整 runtime 的执行脊梁压缩成更适合观察的一小段。&lt;/p&gt;</description></item><item><title>7.13 读 observability 源码树：从 RequestStage 到 exporter</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-observability-source-tree/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-observability-source-tree/</guid><description>&lt;h1 id="读-observability-源码树从-requeststage-到-exporter"&gt;读 observability 源码树：从 RequestStage 到 exporter&lt;a class="anchor" href="#%e8%af%bb-observability-%e6%ba%90%e7%a0%81%e6%a0%91%e4%bb%8e-requeststage-%e5%88%b0-exporter"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;代码导读前面已经教读者如何从入口读到 runtime、如何顺着 protocol 和 &lt;code&gt;io_struct.py&lt;/code&gt; 看对象形状、如何用 &lt;code&gt;bench_one_batch.py&lt;/code&gt; 当显微镜。但整本书后半程已经引入了非常多观测与证据相关的能力：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;RequestStage&lt;/code&gt; / &lt;code&gt;ReqTimeStats&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;metrics collector&lt;/li&gt;
&lt;li&gt;request logger&lt;/li&gt;
&lt;li&gt;request metrics exporter&lt;/li&gt;
&lt;li&gt;watchdog / health check&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果代码导读不把这些源码树重新收一遍，前面的 observability 章节就更像“功能说明”，而不是一条可回到源码的阅读路径。&lt;/p&gt;
&lt;p&gt;这一章就是把 observability 源码树补进代码导读。&lt;/p&gt;
&lt;h2 id="为什么这层值得专门成章"&gt;为什么这层值得专门成章&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e5%b1%82%e5%80%bc%e5%be%97%e4%b8%93%e9%97%a8%e6%88%90%e7%ab%a0"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;因为在复杂 runtime 里，观测代码并不是边角料。很多时候，它才是最能揭示系统边界与作者意图的地方。比如：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;哪些阶段被正式命名为 &lt;code&gt;RequestStage&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;哪些统计被认为值得长期导出&lt;/li&gt;
&lt;li&gt;哪些字段被认为值得落盘&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这些都不是“顺手打几个日志”能表达的。&lt;/p&gt;
&lt;h2 id="一张图observability-源码树本身也有稳定层次"&gt;一张图：observability 源码树本身也有稳定层次&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%beobservability-%e6%ba%90%e7%a0%81%e6%a0%91%e6%9c%ac%e8%ba%ab%e4%b9%9f%e6%9c%89%e7%a8%b3%e5%ae%9a%e5%b1%82%e6%ac%a1"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者看到 &lt;code&gt;observability/&lt;/code&gt; 会只想到 metrics，而不是一棵更完整的证据树。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart TD
 Obs[&amp;#34;observability source tree&amp;#34;] --&amp;gt; Time[&amp;#34;req_time_stats.py / RequestStage&amp;#34;]
 Obs --&amp;gt; Metrics[&amp;#34;metrics_collector.py / scheduler metrics&amp;#34;]
 Obs --&amp;gt; Trace[&amp;#34;trace.py / tracing context&amp;#34;]
 Obs --&amp;gt; Log[&amp;#34;request_logger.py&amp;#34;]
 Obs --&amp;gt; Export[&amp;#34;request_metrics_exporter.py&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：observability 源码树不是单文件功能，而是一组彼此衔接的证据层。&lt;/p&gt;</description></item><item><title>7.14 读 disaggregation 源码树：prefill、decode 与 transfer backend</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-disaggregation-source-tree/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-disaggregation-source-tree/</guid><description>&lt;h1 id="读-disaggregation-源码树prefilldecode-与-transfer-backend"&gt;读 disaggregation 源码树：prefill、decode 与 transfer backend&lt;a class="anchor" href="#%e8%af%bb-disaggregation-%e6%ba%90%e7%a0%81%e6%a0%91prefilldecode-%e4%b8%8e-transfer-backend"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;代码导读前面已经把入口、协议、scheduler、&lt;code&gt;bench_one_batch.py&lt;/code&gt; 和 observability 树都拉回主线了，但还有一棵很容易让读者直接迷路的源码树没有被正式导读：&lt;code&gt;python/sglang/srt/disaggregation/&lt;/code&gt;。这里面同时有：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;prefill.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;decode.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;encode_receiver.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;common/&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;base/&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;fake/&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mooncake/&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;nixl/&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;mori/&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ascend/&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果不把这棵树的阅读顺序讲清楚，它很容易看起来像一堆传输后端杂糅在一起。&lt;/p&gt;
&lt;h2 id="为什么这层值得专门成章"&gt;为什么这层值得专门成章&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e5%b1%82%e5%80%bc%e5%be%97%e4%b8%93%e9%97%a8%e6%88%90%e7%ab%a0"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;因为 disaggregation 在整本书里已经不是边角料了：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;调度章节已经写了 bootstrap/prealloc/transfer 队列&lt;/li&gt;
&lt;li&gt;RequestStage 章节已经写了 transfer 相关阶段&lt;/li&gt;
&lt;li&gt;扩展与调试章节也写了 disaggregation 排障工作流&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;既然它已经进入主线，那么代码导读也应该给它一条清晰的源码阅读路径。&lt;/p&gt;
&lt;h2 id="一张图这棵树不是横向散开的而是有层次的"&gt;一张图：这棵树不是横向散开的，而是有层次的&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%be%e8%bf%99%e6%a3%b5%e6%a0%91%e4%b8%8d%e6%98%af%e6%a8%aa%e5%90%91%e6%95%a3%e5%bc%80%e7%9a%84%e8%80%8c%e6%98%af%e6%9c%89%e5%b1%82%e6%ac%a1%e7%9a%84"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多人第一次看 &lt;code&gt;disaggregation/&lt;/code&gt; 目录时，只能看到很多 backend 名字，却看不出主干层次。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart TD
 Root[&amp;#34;disaggregation/&amp;#34;] --&amp;gt; Core[&amp;#34;prefill.py / decode.py / utils.py&amp;#34;]
 Root --&amp;gt; Base[&amp;#34;base/ / common/&amp;#34;]
 Root --&amp;gt; Backend[&amp;#34;fake / mooncake / nixl / mori / ascend&amp;#34;]
 Root --&amp;gt; Encode[&amp;#34;encode_receiver.py / encode_server.py&amp;#34;]&lt;/pre&gt;&lt;p&gt;这张图比纯文字多解释的一点是：backend 不是主线的起点，真正更稳的入口是 &lt;code&gt;prefill.py&lt;/code&gt; / &lt;code&gt;decode.py&lt;/code&gt; 与 &lt;code&gt;base/common&lt;/code&gt;。&lt;/p&gt;</description></item><item><title>7.15 读 models/ 与 layers/：模型定义与执行砖块</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-models-and-layers-tree/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-models-and-layers-tree/</guid><description>&lt;h1 id="读-models-与-layers模型定义与执行砖块"&gt;读 models/ 与 layers/：模型定义与执行砖块&lt;a class="anchor" href="#%e8%af%bb-models-%e4%b8%8e-layers%e6%a8%a1%e5%9e%8b%e5%ae%9a%e4%b9%89%e4%b8%8e%e6%89%a7%e8%a1%8c%e7%a0%96%e5%9d%97"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;代码导读前面已经给出了从入口到 scheduler、从 observability 到 disaggregation 的多条稳定阅读路径，但还有两棵最容易让人“越看越散”的源码树没有被正式导读：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;python/sglang/srt/models/&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;python/sglang/srt/layers/&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这一章的目标，就是把这两棵树重新收成一条更稳的阅读路径。&lt;/p&gt;
&lt;h2 id="为什么这两棵树特别容易读散"&gt;为什么这两棵树特别容易读散&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e4%b8%a4%e6%a3%b5%e6%a0%91%e7%89%b9%e5%88%ab%e5%ae%b9%e6%98%93%e8%af%bb%e6%95%a3"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;因为它们都很大，而且看起来都很重要：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;models/&lt;/code&gt; 里塞满了具体模型实现&lt;/li&gt;
&lt;li&gt;&lt;code&gt;layers/&lt;/code&gt; 里又塞满了 attention backend、sampler、quantization、moe、rotary embedding 等执行砖块&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果没有阅读策略，读者很容易在这里陷入“文件越多越核心”的错觉，而失去对整本书主线的把握。&lt;/p&gt;
&lt;h2 id="一张图这两棵树在整本书里的角色并不对称"&gt;一张图：这两棵树在整本书里的角色并不对称&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%be%e8%bf%99%e4%b8%a4%e6%a3%b5%e6%a0%91%e5%9c%a8%e6%95%b4%e6%9c%ac%e4%b9%a6%e9%87%8c%e7%9a%84%e8%a7%92%e8%89%b2%e5%b9%b6%e4%b8%8d%e5%af%b9%e7%a7%b0"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会把 &lt;code&gt;models/&lt;/code&gt; 和 &lt;code&gt;layers/&lt;/code&gt; 想成平行目录，而从运行时角度看，它们其实分别站在“模型定义层”和“执行砖块层”。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 Models[&amp;#34;models/&amp;#34;] --&amp;gt; Runner[&amp;#34;ModelRunner&amp;#34;]
 Layers[&amp;#34;layers/&amp;#34;] --&amp;gt; Runner
 Runner --&amp;gt; Worker[&amp;#34;TpModelWorker / draft worker&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：你不应该直接把这两棵树当成“谁都可以先读”的入口，而更适合先知道它们在整体架构里是如何汇合的。&lt;/p&gt;
&lt;h2 id="读-models-的正确心态"&gt;读 &lt;code&gt;models/&lt;/code&gt; 的正确心态&lt;a class="anchor" href="#%e8%af%bb-models-%e7%9a%84%e6%ad%a3%e7%a1%ae%e5%bf%83%e6%80%81"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;models/&lt;/code&gt; 最适合回答的问题不是“系统怎么跑”，而是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;某个模型的特殊语义在哪里实现&lt;/li&gt;
&lt;li&gt;哪些模型是 generation / multimodal / rerank / classification 变体&lt;/li&gt;
&lt;li&gt;哪些模型需要特殊的 logits processor、vision projector、audio path、MTP 分支&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;也就是说，它更像“模型人格差异的具体落点”，而不是执行主循环本身。&lt;/p&gt;
&lt;h2 id="为什么-layers-更像执行砖块仓库"&gt;为什么 &lt;code&gt;layers/&lt;/code&gt; 更像执行砖块仓库&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88-layers-%e6%9b%b4%e5%83%8f%e6%89%a7%e8%a1%8c%e7%a0%96%e5%9d%97%e4%bb%93%e5%ba%93"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;从目录就能看出来，&lt;code&gt;layers/&lt;/code&gt; 至少包括：&lt;/p&gt;</description></item><item><title>7.16 读 distributed/parallel_state.py：并行组真正从哪里来</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-distributed-parallel-state/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-distributed-parallel-state/</guid><description>&lt;h1 id="读-distributedparallel_statepy并行组真正从哪里来"&gt;读 distributed/parallel_state.py：并行组真正从哪里来&lt;a class="anchor" href="#%e8%af%bb-distributedparallel_statepy%e5%b9%b6%e8%a1%8c%e7%bb%84%e7%9c%9f%e6%ad%a3%e4%bb%8e%e5%93%aa%e9%87%8c%e6%9d%a5"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;代码导读前面已经给了 &lt;code&gt;disaggregation/&lt;/code&gt;、&lt;code&gt;scheduler.py&lt;/code&gt;、&lt;code&gt;Engine&lt;/code&gt;、&lt;code&gt;models/&lt;/code&gt; 和 &lt;code&gt;layers/&lt;/code&gt; 的多条阅读路径，但还有一棵对多并行理解极其关键的源码树没有被正式导读：&lt;code&gt;python/sglang/srt/distributed/parallel_state.py&lt;/code&gt; 以及它周围的 &lt;code&gt;distributed/&lt;/code&gt; 目录。&lt;/p&gt;
&lt;p&gt;如果不把这棵树讲清楚，读者会知道 &lt;code&gt;TP&lt;/code&gt; / &lt;code&gt;PP&lt;/code&gt; / world group 存在，却不知道它们在源码里究竟从哪里来、谁在维护它们，以及后续通信算子为什么总是反复去 &lt;code&gt;get_tp_group()&lt;/code&gt;。&lt;/p&gt;
&lt;h2 id="为什么这层值得专门成章"&gt;为什么这层值得专门成章&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e5%b1%82%e5%80%bc%e5%be%97%e4%b8%93%e9%97%a8%e6%88%90%e7%ab%a0"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;因为对于多并行 runtime 来说，这棵树本身就是“通信现实”的总入口。你后面看到的很多东西都在依赖它：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;scheduler / tp_worker 初始化&lt;/li&gt;
&lt;li&gt;&lt;code&gt;communication_op.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;graph capture context&lt;/li&gt;
&lt;li&gt;group-aware all-reduce / all-gather&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;也就是说，这不是只给分布式专家看的目录，而是整本书运行时边界非常重要的一棵树。&lt;/p&gt;
&lt;h2 id="一张图parallel_statepy-在整本系统里更像并行地基"&gt;一张图：&lt;code&gt;parallel_state.py&lt;/code&gt; 在整本系统里更像“并行地基”&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%beparallel_statepy-%e5%9c%a8%e6%95%b4%e6%9c%ac%e7%b3%bb%e7%bb%9f%e9%87%8c%e6%9b%b4%e5%83%8f%e5%b9%b6%e8%a1%8c%e5%9c%b0%e5%9f%ba"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会把 &lt;code&gt;parallel_state.py&lt;/code&gt; 当成后台工具，而不是认为它是后面大量执行边界的地基。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart TD
 Dist[&amp;#34;distributed/parallel_state.py&amp;#34;] --&amp;gt; Groups[&amp;#34;world / tp / pp / moe groups&amp;#34;]
 Groups --&amp;gt; Worker[&amp;#34;scheduler / tp_worker&amp;#34;]
 Groups --&amp;gt; Comm[&amp;#34;communication_op.py&amp;#34;]
 Groups --&amp;gt; Graph[&amp;#34;graph_capture context&amp;#34;]&lt;/pre&gt;&lt;p&gt;这张图比纯文字多解释的一点是：后面很多看似分散的执行与通信路径，实际上都在依赖同一棵地基树。&lt;/p&gt;
&lt;h2 id="为什么应该先读文件头而不是直接找某个-get__group"&gt;为什么应该先读文件头而不是直接找某个 &lt;code&gt;get_*_group()&lt;/code&gt;&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e5%ba%94%e8%af%a5%e5%85%88%e8%af%bb%e6%96%87%e4%bb%b6%e5%a4%b4%e8%80%8c%e4%b8%8d%e6%98%af%e7%9b%b4%e6%8e%a5%e6%89%be%e6%9f%90%e4%b8%aa-get__group"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;文件开头已经把 workflow 写得很清楚：&lt;/p&gt;</description></item><item><title>7.17 读 parser/ 与 function_call/：模板、reasoning 与 detector 树</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-parser-and-function-call-tree/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-parser-and-function-call-tree/</guid><description>&lt;h1 id="读-parser-与-function_call模板reasoning-与-detector-树"&gt;读 parser/ 与 function_call/：模板、reasoning 与 detector 树&lt;a class="anchor" href="#%e8%af%bb-parser-%e4%b8%8e-function_call%e6%a8%a1%e6%9d%bfreasoning-%e4%b8%8e-detector-%e6%a0%91"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;代码导读前面已经把 &lt;code&gt;entrypoints/openai&lt;/code&gt;、&lt;code&gt;protocol.py&lt;/code&gt;、&lt;code&gt;io_struct.py&lt;/code&gt;、&lt;code&gt;observability/&lt;/code&gt;、&lt;code&gt;disaggregation/&lt;/code&gt; 和 &lt;code&gt;models/&lt;/code&gt; / &lt;code&gt;layers/&lt;/code&gt; 树都串起来了，但还有两棵和结构化生成强相关、同时又特别容易让人读散的目录没有正式导读：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;python/sglang/srt/parser/&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;python/sglang/srt/function_call/&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这一章的目标，就是把这两棵树重新收成一条稳健的阅读顺序。&lt;/p&gt;
&lt;h2 id="为什么这两棵树容易读散"&gt;为什么这两棵树容易读散&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e4%b8%a4%e6%a3%b5%e6%a0%91%e5%ae%b9%e6%98%93%e8%af%bb%e6%95%a3"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;因为它们都不是单一主题目录：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;parser/&lt;/code&gt; 里既有 conversation/template，也有 reasoning parser、harmony parser、code completion parser、jinja utilities&lt;/li&gt;
&lt;li&gt;&lt;code&gt;function_call/&lt;/code&gt; 里既有通用 parser，也有很多模型专属 detector&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果没有导读，读者很容易在 detector 名字和模板工具之间来回跳，却始终不知道这两棵树在整本系统里的角色。&lt;/p&gt;
&lt;h2 id="一张图这两棵树分别站在解释和检测两侧"&gt;一张图：这两棵树分别站在“解释”和“检测”两侧&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%be%e8%bf%99%e4%b8%a4%e6%a3%b5%e6%a0%91%e5%88%86%e5%88%ab%e7%ab%99%e5%9c%a8%e8%a7%a3%e9%87%8a%e5%92%8c%e6%a3%80%e6%b5%8b%e4%b8%a4%e4%be%a7"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多人会把 parser 和 function_call 混成一个目录层，而从系统角度看，它们分别承担不同工作。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 Parser[&amp;#34;parser/&amp;#34;] --&amp;gt; Surface[&amp;#34;template / reasoning / conversation interpretation&amp;#34;]
 FC[&amp;#34;function_call/&amp;#34;] --&amp;gt; Detect[&amp;#34;tool-call detector / constraint builder&amp;#34;]
 Surface --&amp;gt; OpenAI[&amp;#34;serving_chat / responses&amp;#34;]
 Detect --&amp;gt; OpenAI&lt;/pre&gt;&lt;p&gt;这张图比纯文字多解释的一点是：一棵树更偏解释输入与上下文，一棵树更偏解释模型输出里的工具调用模式。&lt;/p&gt;
&lt;h2 id="为什么应该先读-parser"&gt;为什么应该先读 &lt;code&gt;parser/&lt;/code&gt;&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e5%ba%94%e8%af%a5%e5%85%88%e8%af%bb-parser"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;因为它更像“进入 structured / tool / reasoning 语义之前的解释层”。&lt;/p&gt;</description></item><item><title>7.18 读 http_server.py：接口森林与 app.state 装配</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-http-server-route-forest/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-http-server-route-forest/</guid><description>&lt;h1 id="读-http_serverpy接口森林与-appstate-装配"&gt;读 http_server.py：接口森林与 app.state 装配&lt;a class="anchor" href="#%e8%af%bb-http_serverpy%e6%8e%a5%e5%8f%a3%e6%a3%ae%e6%9e%97%e4%b8%8e-appstate-%e8%a3%85%e9%85%8d"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;代码导读前面已经单独讲了 &lt;code&gt;entrypoints/openai&lt;/code&gt;、&lt;code&gt;Engine&lt;/code&gt;、&lt;code&gt;protocol.py&lt;/code&gt; 和分布式地基树，但还有一个文件虽然被频繁提到，却仍然值得单独成章：&lt;code&gt;python/sglang/srt/entrypoints/http_server.py&lt;/code&gt;。这是整套系统最像“接口森林”的地方，里面不仅挂着 OpenAI-compatible 路由，还挂着：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Responses API&lt;/li&gt;
&lt;li&gt;Ollama-compatible 接口&lt;/li&gt;
&lt;li&gt;Anthropic-compatible 接口&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/freeze_gc&lt;/code&gt;、&lt;code&gt;/set_internal_state&lt;/code&gt;、&lt;code&gt;/open_session&lt;/code&gt; 等维护端点&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这一章的目标，就是把这棵森林重新整理成一条更稳的阅读顺序。&lt;/p&gt;
&lt;h2 id="为什么这个文件特别容易读散"&gt;为什么这个文件特别容易读散&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e4%b8%aa%e6%96%87%e4%bb%b6%e7%89%b9%e5%88%ab%e5%ae%b9%e6%98%93%e8%af%bb%e6%95%a3"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;因为它同时承担了两类工作：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;在 &lt;code&gt;lifespan&lt;/code&gt; / 初始化阶段把各种 serving handler 放到 &lt;code&gt;app.state&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;在后半段挂一整片 HTTP 路由森林&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;如果你没有先建立“初始化森林”和“路由森林”这两层区分，就很容易把这个文件读成一长串路由定义，而不是系统对外表面的总入口。&lt;/p&gt;
&lt;h2 id="一张图http_serverpy-其实在做两件事"&gt;一张图：&lt;code&gt;http_server.py&lt;/code&gt; 其实在做两件事&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%behttp_serverpy-%e5%85%b6%e5%ae%9e%e5%9c%a8%e5%81%9a%e4%b8%a4%e4%bb%b6%e4%ba%8b"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会把它当成一个单纯路由文件，而忽略前半段的 handler 装配和后半段的路由映射是两层不同逻辑。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart TD
 Init[&amp;#34;lifespan / app.state initialization&amp;#34;] --&amp;gt; Handlers[&amp;#34;OpenAI / Responses / Ollama / Anthropic handlers&amp;#34;]
 Handlers --&amp;gt; Routes[&amp;#34;HTTP route forest&amp;#34;]&lt;/pre&gt;&lt;p&gt;这张图比纯文字多解释的一点是：你读这个文件时，应该先看“装了哪些 handler”，再看“这些 handler 分别挂在哪些路径上”。&lt;/p&gt;
&lt;h2 id="为什么应该先看-appstate-初始化"&gt;为什么应该先看 &lt;code&gt;app.state&lt;/code&gt; 初始化&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e5%ba%94%e8%af%a5%e5%85%88%e7%9c%8b-appstate-%e5%88%9d%e5%a7%8b%e5%8c%96"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;http_server.py&lt;/code&gt; 很早就会把这些对象挂到 &lt;code&gt;app.state&lt;/code&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;openai_serving_completion&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;openai_serving_chat&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;openai_serving_embedding&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;openai_serving_classify&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;openai_serving_score&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;openai_serving_rerank&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;openai_serving_responses&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ollama_serving&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;anthropic_serving&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这一步特别重要，因为它说明后面的路由并不直接实现逻辑，而是只是把请求转给一组已经装配好的 handler。也就是说，路由层更像调度表，而不是行为本体。&lt;/p&gt;</description></item><item><title>7.19 读 launch_server.py：模式分叉怎样决定全书入口</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-launch-server-and-mode-switches/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-launch-server-and-mode-switches/</guid><description>&lt;h1 id="读-launch_serverpy模式分叉怎样决定全书入口"&gt;读 launch_server.py：模式分叉怎样决定全书入口&lt;a class="anchor" href="#%e8%af%bb-launch_serverpy%e6%a8%a1%e5%bc%8f%e5%88%86%e5%8f%89%e6%80%8e%e6%a0%b7%e5%86%b3%e5%ae%9a%e5%85%a8%e4%b9%a6%e5%85%a5%e5%8f%a3"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的代码导读已经覆盖了 &lt;code&gt;http_server.py&lt;/code&gt;、OpenAI entrypoints、&lt;code&gt;protocol.py&lt;/code&gt;、&lt;code&gt;scheduler.py&lt;/code&gt; 和 observability 源码树，但还有一个短小却特别关键的入口文件值得单独讲：&lt;code&gt;python/sglang/launch_server.py&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;它的代码量不大，却决定了整本书最开始那条“从外到内”的阅读路径究竟会走向哪一种 runtime 人格：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;编码器分离式服务&lt;/li&gt;
&lt;li&gt;gRPC 模式&lt;/li&gt;
&lt;li&gt;Ray 模式&lt;/li&gt;
&lt;li&gt;默认 HTTP + SRT 模式&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果你忽略这个入口文件，很容易把整本书都读成“默认 HTTP server 的说明书”，但上游源码实际上已经把多种启动人格放进同一个启动分叉里。&lt;/p&gt;
&lt;h2 id="为什么短入口文件也值得技术书单独成章"&gt;为什么短入口文件也值得技术书单独成章&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e7%9f%ad%e5%85%a5%e5%8f%a3%e6%96%87%e4%bb%b6%e4%b9%9f%e5%80%bc%e5%be%97%e6%8a%80%e6%9c%af%e4%b9%a6%e5%8d%95%e7%8b%ac%e6%88%90%e7%ab%a0"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这是计算机书里很常见的一种误判：读者会高估大文件、低估小入口。可是在复杂系统里，短入口文件往往承担的是“路由全局心智模型”的角色。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;launch_server.py&lt;/code&gt; 的价值不是实现细节多，而是它用非常少的分支告诉你：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;哪些运行模式属于同一家族&lt;/li&gt;
&lt;li&gt;哪些模式会走完全不同的入口文件&lt;/li&gt;
&lt;li&gt;默认路径到底落到哪一套子系统&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;从教学角度看，它像整本书的目录索引器。&lt;/p&gt;
&lt;h2 id="一张图run_serverserver_args-怎样把系统送进不同人格"&gt;一张图：&lt;code&gt;run_server(server_args)&lt;/code&gt; 怎样把系统送进不同人格&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%berun_serverserver_args-%e6%80%8e%e6%a0%b7%e6%8a%8a%e7%b3%bb%e7%bb%9f%e9%80%81%e8%bf%9b%e4%b8%8d%e5%90%8c%e4%ba%ba%e6%a0%bc"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会默认 &lt;code&gt;sglang serve&lt;/code&gt; 最终总会落到 &lt;code&gt;entrypoints/http_server.py&lt;/code&gt;，但源码并不是这样写的。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart TD
 Args[&amp;#34;prepare_server_args(...)&amp;#34;] --&amp;gt; Run[&amp;#34;run_server(server_args)&amp;#34;]
 Run --&amp;gt; Enc[&amp;#34;encoder_only ?&amp;#34;]
 Enc --&amp;gt;|yes + grpc| EncGrpc[&amp;#34;disaggregation.encode_grpc_server.serve_grpc_encoder&amp;#34;]
 Enc --&amp;gt;|yes + no grpc| EncHttp[&amp;#34;disaggregation.encode_server.launch_server&amp;#34;]
 Run --&amp;gt;|no| Grpc[&amp;#34;grpc_mode ?&amp;#34;]
 Grpc --&amp;gt;|yes| GrpcSrv[&amp;#34;entrypoints.grpc_server.serve_grpc&amp;#34;]
 Grpc --&amp;gt;|no| Ray[&amp;#34;use_ray ?&amp;#34;]
 Ray --&amp;gt;|yes| RaySrv[&amp;#34;ray.http_server.launch_server&amp;#34;]
 Ray --&amp;gt;|no| Http[&amp;#34;entrypoints.http_server.launch_server&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：默认 HTTP 路径只是分叉树的一枝，而不是唯一现实。&lt;/p&gt;</description></item><item><title>7.20 读 tokenizer_manager.py：入口、状态桥与回包收敛</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-tokenizer-manager-as-bridge/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-tokenizer-manager-as-bridge/</guid><description>&lt;h1 id="读-tokenizer_managerpy入口状态桥与回包收敛"&gt;读 tokenizer_manager.py：入口、状态桥与回包收敛&lt;a class="anchor" href="#%e8%af%bb-tokenizer_managerpy%e5%85%a5%e5%8f%a3%e7%8a%b6%e6%80%81%e6%a1%a5%e4%b8%8e%e5%9b%9e%e5%8c%85%e6%94%b6%e6%95%9b"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的代码导读已经覆盖了 &lt;code&gt;http_server.py&lt;/code&gt;、OpenAI entrypoints、&lt;code&gt;protocol.py&lt;/code&gt;、&lt;code&gt;Engine&lt;/code&gt; API 和 &lt;code&gt;scheduler.py&lt;/code&gt;，但还有一个全书几乎处处会提到、却还没有被单独当作“稳定阅读入口”的文件：&lt;code&gt;python/sglang/srt/managers/tokenizer_manager.py&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;这是一个很容易被读散的文件。因为它同时承担了：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;请求入口&lt;/li&gt;
&lt;li&gt;请求状态表维护&lt;/li&gt;
&lt;li&gt;向 scheduler 发送对象&lt;/li&gt;
&lt;li&gt;从 detokenizer 收结果&lt;/li&gt;
&lt;li&gt;abort / pause / session / profile / weights / LoRA 等控制面&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果没有一章专门讲“该怎样读这棵树”，读者很容易把它看成一个什么都做的巨型 manager，最后只能记住一些函数名，却很难形成稳定心智模型。&lt;/p&gt;
&lt;p&gt;这章的目标，就是把 &lt;code&gt;tokenizer_manager.py&lt;/code&gt; 压成一条可读的骨架。&lt;/p&gt;
&lt;h2 id="为什么这棵树值得单独成章"&gt;为什么这棵树值得单独成章&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e6%a3%b5%e6%a0%91%e5%80%bc%e5%be%97%e5%8d%95%e7%8b%ac%e6%88%90%e7%ab%a0"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;从全书主线看，&lt;code&gt;TokenizerManager&lt;/code&gt; 处在一个非常特殊的位置：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;request lifecycle 里，它是入口与回包的首尾收敛点&lt;/li&gt;
&lt;li&gt;runtime architecture 里，它是主进程控制面的核心 manager&lt;/li&gt;
&lt;li&gt;extension/debugging 里，很多 live control 都最终会落到它&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这意味着它不是某个局部机制的实现文件，而是一棵真正的“交叉节点源码树”。优秀技术书在这里不能只说“去看看 tokenizer_manager.py”，而应该给出一个正式的阅读框架。&lt;/p&gt;
&lt;h2 id="一张图为什么-tokenizermanager-是全书级桥梁文件"&gt;一张图：为什么 &lt;code&gt;TokenizerManager&lt;/code&gt; 是全书级桥梁文件&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%be%e4%b8%ba%e4%bb%80%e4%b9%88-tokenizermanager-%e6%98%af%e5%85%a8%e4%b9%a6%e7%ba%a7%e6%a1%a5%e6%a2%81%e6%96%87%e4%bb%b6"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者知道 &lt;code&gt;TokenizerManager&lt;/code&gt; 很重要，但不知道它重要在哪几条轴线上。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart TD
 HTTP[&amp;#34;HTTP / Engine / surface entry&amp;#34;] --&amp;gt; Gen[&amp;#34;generate_request(...)&amp;#34;]
 Gen --&amp;gt; State[&amp;#34;rid_to_state + ReqState&amp;#34;]
 State --&amp;gt; Send[&amp;#34;send_to_scheduler&amp;#34;]
 Send --&amp;gt; Wait[&amp;#34;_wait_one_response(...)&amp;#34;]
 Wait --&amp;gt; Recv[&amp;#34;handle_loop / recv_from_detokenizer&amp;#34;]
 Recv --&amp;gt; Out[&amp;#34;response / metrics / request log&amp;#34;]
 Control[&amp;#34;abort / pause / profile / weights / LoRA&amp;#34;] --&amp;gt; Send
 Control --&amp;gt; State&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：&lt;code&gt;TokenizerManager&lt;/code&gt; 不是单向入口，而是一个双向桥，同时还挂着很多控制面分支。&lt;/p&gt;</description></item><item><title>7.21 读输出尾部：scheduler output processor 与 detokenizer</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-output-tail-pipeline/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-output-tail-pipeline/</guid><description>&lt;h1 id="读输出尾部scheduler-output-processor-与-detokenizer"&gt;读输出尾部：scheduler output processor 与 detokenizer&lt;a class="anchor" href="#%e8%af%bb%e8%be%93%e5%87%ba%e5%b0%be%e9%83%a8scheduler-output-processor-%e4%b8%8e-detokenizer"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的代码导读已经把 &lt;code&gt;TokenizerManager&lt;/code&gt; 收成了一棵正式的桥梁树，但还有一段同样关键、却更容易被忽略的尾部链路没有被单独讲清：模型前向结束、sampler 给出下一个 token 之后，系统到底怎样把这些执行层结果整理成可回包的输出？这里真正负责收尾的并不是单个函数，而是两棵紧密咬合的树：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;scheduler_output_processor_mixin.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;detokenizer_manager.py&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果不把这两棵树合起来读，很多读者会在 execution model 章节里知道 &lt;code&gt;BatchTokenIDOutput&lt;/code&gt; 和 &lt;code&gt;BatchStrOutput&lt;/code&gt; 这两个名字，却不知道它们在源码里怎样被生产、传递和消费。&lt;/p&gt;
&lt;p&gt;这章的目标，就是把输出尾部压成一条稳定的阅读路径。&lt;/p&gt;
&lt;h2 id="为什么这两棵树应该配对阅读"&gt;为什么这两棵树应该配对阅读&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e4%b8%a4%e6%a3%b5%e6%a0%91%e5%ba%94%e8%af%a5%e9%85%8d%e5%af%b9%e9%98%85%e8%af%bb"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;单读 &lt;code&gt;scheduler_output_processor_mixin.py&lt;/code&gt;，你会看到：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;output ids 怎样写回 request state&lt;/li&gt;
&lt;li&gt;&lt;code&gt;finish_reason&lt;/code&gt; 怎样被检查&lt;/li&gt;
&lt;li&gt;output logprobs 与 reasoning token 怎样被附着&lt;/li&gt;
&lt;li&gt;&lt;code&gt;BatchTokenIDOutput&lt;/code&gt; 怎样发往下游&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;单读 &lt;code&gt;detokenizer_manager.py&lt;/code&gt;，你会看到：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;BatchTokenIDOutput&lt;/code&gt; 怎样被 decode&lt;/li&gt;
&lt;li&gt;stop trim 怎样发生&lt;/li&gt;
&lt;li&gt;&lt;code&gt;decode_status&lt;/code&gt; 怎样跨 chunk 保存状态&lt;/li&gt;
&lt;li&gt;&lt;code&gt;BatchStrOutput&lt;/code&gt; 怎样再送回 tokenizer 侧&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;真正的工程意义只会在两者配对时出现：前者把“执行结果”整理成“token 级输出对象”，后者再把“token 级输出对象”整理成“文本级输出对象”。这就是整个 runtime 尾部收敛的核心。&lt;/p&gt;
&lt;h2 id="一张图输出尾部真正发生了什么"&gt;一张图：输出尾部真正发生了什么&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%be%e8%be%93%e5%87%ba%e5%b0%be%e9%83%a8%e7%9c%9f%e6%ad%a3%e5%8f%91%e7%94%9f%e4%ba%86%e4%bb%80%e4%b9%88"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会自然地把“sample 完了”当成输出已经结束，但源码里 sample 之后还有一整条正式 pipeline。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 Exec[&amp;#34;forward + sample&amp;#34;] --&amp;gt; Proc[&amp;#34;scheduler_output_processor_mixin&amp;#34;]
 Proc --&amp;gt; Tok[&amp;#34;BatchTokenIDOutput&amp;#34;]
 Tok --&amp;gt; IPC[&amp;#34;send_to_detokenizer.send_output(...)&amp;#34;]
 IPC --&amp;gt; Det[&amp;#34;DetokenizerManager.event_loop()&amp;#34;]
 Det --&amp;gt; Str[&amp;#34;BatchStrOutput&amp;#34;]
 Str --&amp;gt; TM[&amp;#34;TokenizerManager handle_loop&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：sample 之后并不是立刻回到调用方，中间还有一个 token 输出层和一个文本收口层。&lt;/p&gt;</description></item><item><title>7.22 读 io_struct.py：请求对象、批对象与输出对象家族</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-io-struct-object-families/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-io-struct-object-families/</guid><description>&lt;h1 id="读-io_structpy请求对象批对象与输出对象家族"&gt;读 io_struct.py：请求对象、批对象与输出对象家族&lt;a class="anchor" href="#%e8%af%bb-io_structpy%e8%af%b7%e6%b1%82%e5%af%b9%e8%b1%a1%e6%89%b9%e5%af%b9%e8%b1%a1%e4%b8%8e%e8%be%93%e5%87%ba%e5%af%b9%e8%b1%a1%e5%ae%b6%e6%97%8f"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的代码导读已经在 &lt;a href="./protocol-and-io-struct-bridge/"&gt;7.9 读 &lt;code&gt;protocol.py&lt;/code&gt; 与 &lt;code&gt;io_struct.py&lt;/code&gt;&lt;/a&gt; 里解释了“公开协议怎样变成内部对象”，但还有一层更适合单独讲清的内容：&lt;code&gt;io_struct.py&lt;/code&gt; 自己并不是一堆零散 dataclass，而是一套层次非常稳定的对象家族。&lt;/p&gt;
&lt;p&gt;如果只知道：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;GenerateReqInput&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;TokenizedGenerateReqInput&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;BatchTokenIDOutput&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;BatchStrOutput&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这些名字存在，却不知道它们为什么要分成这么多层，那么整本书前面建立的 request lifecycle、调度、执行和输出链路，到了源码级就仍然会显得割裂。&lt;/p&gt;
&lt;p&gt;这章的目标，就是把 &lt;code&gt;io_struct.py&lt;/code&gt; 压成一张对象家族地图。&lt;/p&gt;
&lt;h2 id="为什么-io_structpy-值得再单独成章"&gt;为什么 &lt;code&gt;io_struct.py&lt;/code&gt; 值得再单独成章&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88-io_structpy-%e5%80%bc%e5%be%97%e5%86%8d%e5%8d%95%e7%8b%ac%e6%88%90%e7%ab%a0"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;7.9 更关注的是“协议桥”，也就是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;protocol.py&lt;/code&gt; 里的对外 schema&lt;/li&gt;
&lt;li&gt;&lt;code&gt;serving_*._convert_to_internal_request(...)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;io_struct.py&lt;/code&gt; 里的内部对象&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;而这章更关注 &lt;code&gt;io_struct.py&lt;/code&gt; 内部自身的分层逻辑：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;哪些对象面向入口请求&lt;/li&gt;
&lt;li&gt;哪些对象面向 tokenized request&lt;/li&gt;
&lt;li&gt;哪些对象面向 batch&lt;/li&gt;
&lt;li&gt;哪些对象面向输出与控制面&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;换句话说，上一章讲“怎么进来”，这一章讲“进来之后对象怎样沿 runtime 主线继续演化”。&lt;/p&gt;
&lt;h2 id="一张图io_structpy-里的对象不是平铺列表而是家族树"&gt;一张图：&lt;code&gt;io_struct.py&lt;/code&gt; 里的对象不是平铺列表，而是家族树&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%beio_structpy-%e9%87%8c%e7%9a%84%e5%af%b9%e8%b1%a1%e4%b8%8d%e6%98%af%e5%b9%b3%e9%93%ba%e5%88%97%e8%a1%a8%e8%80%8c%e6%98%af%e5%ae%b6%e6%97%8f%e6%a0%91"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者第一次打开 &lt;code&gt;io_struct.py&lt;/code&gt;，会把它看成一大串 dataclass 清单，但源码其实已经按 runtime 阶段自然分层。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart TD
 Base[&amp;#34;BaseReq / BaseBatchReq&amp;#34;] --&amp;gt; Req[&amp;#34;GenerateReqInput / EmbeddingReqInput&amp;#34;]
 Req --&amp;gt; Tok[&amp;#34;Tokenized*ReqInput&amp;#34;]
 Tok --&amp;gt; Batch[&amp;#34;BatchTokenized*ReqInput&amp;#34;]
 Batch --&amp;gt; Exec[&amp;#34;Schedule / Forward / runtime processing&amp;#34;]
 Exec --&amp;gt; Out1[&amp;#34;BatchTokenIDOutput&amp;#34;]
 Out1 --&amp;gt; Out2[&amp;#34;BatchStrOutput / BatchEmbeddingOutput&amp;#34;]
 Base --&amp;gt; Ctl[&amp;#34;AbortReq / PauseGenerationReqInput / OpenSessionReqInput ...&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：&lt;code&gt;io_struct.py&lt;/code&gt; 里的类不是按“功能模块”随意摆放，而是按 runtime 阶段分层。&lt;/p&gt;</description></item><item><title>7.23 读 detokenizer_manager.py：decode_status、stop trim 与文本收口</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-detokenizer-manager/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-detokenizer-manager/</guid><description>&lt;h1 id="读-detokenizer_managerpydecode_statusstop-trim-与文本收口"&gt;读 detokenizer_manager.py：decode_status、stop trim 与文本收口&lt;a class="anchor" href="#%e8%af%bb-detokenizer_managerpydecode_statusstop-trim-%e4%b8%8e%e6%96%87%e6%9c%ac%e6%94%b6%e5%8f%a3"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的代码导读已经在 &lt;a href="./reading-output-tail-pipeline/"&gt;7.21 读输出尾部&lt;/a&gt; 里把 &lt;code&gt;scheduler_output_processor_mixin.py&lt;/code&gt; 和 &lt;code&gt;detokenizer_manager.py&lt;/code&gt; 配对成了一条尾部链路，但如果你真的打开 &lt;code&gt;python/sglang/srt/managers/detokenizer_manager.py&lt;/code&gt;，仍然会遇到一个具体问题：这棵文件树本身应该怎么读？&lt;/p&gt;
&lt;p&gt;因为这棵树里虽然只有一条主 loop，但它同时牵涉：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;decode_status&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;_grouped_batch_decode(...)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;trim_matched_stop(...)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;BatchTokenIDOutput -&amp;gt; BatchStrOutput&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;watchdog 与多 worker detokenizer 路径&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果没有一章专门把“面对这棵文件树时该先抓什么”讲清楚，读者很容易只记住“它会 decode”，却不知道它到底吸收了哪些尾部复杂度。&lt;/p&gt;
&lt;p&gt;这章的目标，就是把 &lt;code&gt;detokenizer_manager.py&lt;/code&gt; 本身压成一条可读的骨架。&lt;/p&gt;
&lt;h2 id="为什么这棵树值得单独成章"&gt;为什么这棵树值得单独成章&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e6%a3%b5%e6%a0%91%e5%80%bc%e5%be%97%e5%8d%95%e7%8b%ac%e6%88%90%e7%ab%a0"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;在全书主线里，&lt;code&gt;DetokenizerManager&lt;/code&gt; 很容易被误判成“尾部工具进程”，但源码显示它承担的是更正式的角色：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;它有自己的 IPC 边界&lt;/li&gt;
&lt;li&gt;有自己的状态表 &lt;code&gt;decode_status&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;有自己的 dispatcher&lt;/li&gt;
&lt;li&gt;有自己的 soft watchdog&lt;/li&gt;
&lt;li&gt;还负责 stop trim 与 grouped batch decode&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;也就是说，它不是“把 token 转成文本”这么简单，而是 runtime 输出尾部的专门收口层。&lt;/p&gt;
&lt;p&gt;从技术书角度看，这种“看似单一职责，实则吸收大量尾部复杂度”的文件，特别值得单独做导读。&lt;/p&gt;
&lt;h2 id="一张图detokenizer_managerpy-在输出尾部的真实位置"&gt;一张图：&lt;code&gt;detokenizer_manager.py&lt;/code&gt; 在输出尾部的真实位置&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%bedetokenizer_managerpy-%e5%9c%a8%e8%be%93%e5%87%ba%e5%b0%be%e9%83%a8%e7%9a%84%e7%9c%9f%e5%ae%9e%e4%bd%8d%e7%bd%ae"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者知道它位于 scheduler 和 tokenizer 之间，但不知道它到底把哪些复杂度截留在自己这里。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 Tok[&amp;#34;BatchTokenIDOutput&amp;#34;] --&amp;gt; Loop[&amp;#34;DetokenizerManager.event_loop()&amp;#34;]
 Loop --&amp;gt; State[&amp;#34;decode_status&amp;#34;]
 State --&amp;gt; Group[&amp;#34;_grouped_batch_decode(...)&amp;#34;]
 Group --&amp;gt; Trim[&amp;#34;trim_matched_stop(...)&amp;#34;]
 Trim --&amp;gt; Str[&amp;#34;BatchStrOutput&amp;#34;]
 Str --&amp;gt; Back[&amp;#34;send_to_tokenizer.send_pyobj(...)&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：detokenizer 的真正价值不只是 decode，而是“带状态地 decode、trim、整理并回送”。&lt;/p&gt;</description></item><item><title>7.24 读 multi_tokenizer_mixin.py：多 worker 入口与回包拆分</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-multi-tokenizer-routing/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-multi-tokenizer-routing/</guid><description>&lt;h1 id="读-multi_tokenizer_mixinpy多-worker-入口与回包拆分"&gt;读 multi_tokenizer_mixin.py：多 worker 入口与回包拆分&lt;a class="anchor" href="#%e8%af%bb-multi_tokenizer_mixinpy%e5%a4%9a-worker-%e5%85%a5%e5%8f%a3%e4%b8%8e%e5%9b%9e%e5%8c%85%e6%8b%86%e5%88%86"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的 request lifecycle 已经在 &lt;a href="../request-lifecycle/multi-worker-routing-and-http-surfaces/"&gt;2.6 多 tokenizer worker、Router 与 HTTP 入口放大&lt;/a&gt; 里解释了多 worker 模式的主路径，但如果你真正打开源码，会发现这条支线最容易读散的文件其实是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;python/sglang/srt/managers/multi_tokenizer_mixin.py&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这棵树同时承载了三件事：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;TokenizerWorker&lt;/code&gt; 怎样给请求附着 worker 身份&lt;/li&gt;
&lt;li&gt;&lt;code&gt;MultiTokenizerRouter&lt;/code&gt; 怎样把多入口请求汇总到 shared scheduler&lt;/li&gt;
&lt;li&gt;detokenizer 回包怎样再按 worker 拆回去&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果没有一章专门讲“这棵树该怎么读”，多 worker 支线在源码层仍然会显得像几段互不相关的胶水代码。&lt;/p&gt;
&lt;p&gt;这章的目标，就是把多 worker 入口和回包拆分的源码骨架正式讲清楚。&lt;/p&gt;
&lt;h2 id="为什么这棵树值得单独成章"&gt;为什么这棵树值得单独成章&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e6%a3%b5%e6%a0%91%e5%80%bc%e5%be%97%e5%8d%95%e7%8b%ac%e6%88%90%e7%ab%a0"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这是一个典型的“看起来像实现细节，实际上决定系统拓扑边界”的文件。&lt;/p&gt;
&lt;p&gt;如果你只看 &lt;code&gt;http_server.py&lt;/code&gt;，你能知道多 worker 模式存在；但真正决定：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;请求如何标记来源 worker&lt;/li&gt;
&lt;li&gt;输出如何按来源 worker 再拆开&lt;/li&gt;
&lt;li&gt;socket 怎样懒注册和复用&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;的核心逻辑，在 &lt;code&gt;multi_tokenizer_mixin.py&lt;/code&gt; 里。&lt;/p&gt;
&lt;p&gt;从技术书角度看，这种文件特别值得做成导读章，因为它解释的不是单个算法，而是系统拓扑在源码层怎样落地。&lt;/p&gt;
&lt;h2 id="一张图多-worker-源码树真正要解决什么"&gt;一张图：多 worker 源码树真正要解决什么&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%be%e5%a4%9a-worker-%e6%ba%90%e7%a0%81%e6%a0%91%e7%9c%9f%e6%ad%a3%e8%a6%81%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：多 worker 模式看起来像“多个 tokenizer worker + 一个 router”，但源码里的关键难点其实是如何把输出准确送回原始 worker。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 HW1[&amp;#34;HTTP worker / TokenizerWorker 1&amp;#34;] --&amp;gt; Tag[&amp;#34;attach http_worker_ipc&amp;#34;]
 HW2[&amp;#34;HTTP worker / TokenizerWorker 2&amp;#34;] --&amp;gt; Tag
 Tag --&amp;gt; Router[&amp;#34;MultiTokenizerRouter&amp;#34;]
 Router --&amp;gt; Sch[&amp;#34;shared Scheduler&amp;#34;]
 Sch --&amp;gt; Det[&amp;#34;DetokenizerManager&amp;#34;]
 Det --&amp;gt; Split[&amp;#34;_handle_output_by_index(...)&amp;#34;]
 Split --&amp;gt; Sock[&amp;#34;SocketMapping.send_output(...)&amp;#34;]
 Sock --&amp;gt; HW1
 Sock --&amp;gt; HW2&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：多 worker 的真正复杂度不在“多开几个入口”，而在“同一批输出怎样按 worker 精确扇回去”。&lt;/p&gt;</description></item><item><title>7.25 读 session_controller.py 与 SessionAwareCache</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-session-controller-and-session-cache/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-session-controller-and-session-cache/</guid><description>&lt;h1 id="读-session_controllerpy-与-sessionawarecache"&gt;读 session_controller.py 与 SessionAwareCache&lt;a class="anchor" href="#%e8%af%bb-session_controllerpy-%e4%b8%8e-sessionawarecache"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的 request lifecycle 已经在 &lt;a href="../request-lifecycle/session-state-timeouts-and-aborts/"&gt;2.3 会话、超时与中止路径&lt;/a&gt; 里解释了 session 为什么会改变请求生命周期，调度与内存部分也在 &lt;a href="../scheduling-and-memory/runtime-checkers-and-session-aware-cache/"&gt;4.5 Runtime checker、SessionAwareCache 与内存不变量&lt;/a&gt; 里解释了 &lt;code&gt;SessionAwareCache&lt;/code&gt; 为什么要接管 streaming session 的 KV 生命周期。但如果你真正打开源码，仍然会遇到一个更具体的问题：&lt;/p&gt;
&lt;p&gt;session 相关实现到底应该从哪几棵树开始读，怎样把它们接起来？&lt;/p&gt;
&lt;p&gt;这章的目标，就是把：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;session_controller.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;session_aware_cache.py&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;收成一条稳定的源码阅读路径。&lt;/p&gt;
&lt;h2 id="为什么这两棵树必须配对读"&gt;为什么这两棵树必须配对读&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e4%b8%a4%e6%a3%b5%e6%a0%91%e5%bf%85%e9%a1%bb%e9%85%8d%e5%af%b9%e8%af%bb"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;单读 &lt;code&gt;session_controller.py&lt;/code&gt;，你会知道：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;session 怎样被打开 / 关闭&lt;/li&gt;
&lt;li&gt;append / replace / drop_previous_output 怎样改变请求链&lt;/li&gt;
&lt;li&gt;streaming session 有哪些限制&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;单读 &lt;code&gt;session_aware_cache.py&lt;/code&gt;，你会知道：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;SessionSlot&lt;/code&gt; 怎样接管 &lt;code&gt;req_pool_idx&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;match_prefix(...)&lt;/code&gt; 怎样恢复会话态 KV&lt;/li&gt;
&lt;li&gt;&lt;code&gt;release_session(...)&lt;/code&gt; 怎样释放 slot 占用的资源&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;但只有把两者配在一起，你才会真正看清一件事：&lt;/p&gt;
&lt;p&gt;session 不只是“请求之间共享逻辑上下文”，它还会改变 KV 所有权与缓存生命周期。&lt;/p&gt;
&lt;p&gt;这就是为什么它不是一个轻量 API 特性，而是一条贯穿请求、缓存和回收的系统支线。&lt;/p&gt;
&lt;h2 id="一张图session-在源码里并不是单树问题"&gt;一张图：session 在源码里并不是单树问题&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%besession-%e5%9c%a8%e6%ba%90%e7%a0%81%e9%87%8c%e5%b9%b6%e4%b8%8d%e6%98%af%e5%8d%95%e6%a0%91%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会把 session 理解成 &lt;code&gt;SessionController&lt;/code&gt; 自己的一棵树，但真正的实现其实横跨请求链和缓存链。&lt;/p&gt;</description></item><item><title>7.26 读 health_generate：探活请求怎样穿过 runtime</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-health-check-path/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-health-check-path/</guid><description>&lt;h1 id="读-health_generate探活请求怎样穿过-runtime"&gt;读 health_generate：探活请求怎样穿过 runtime&lt;a class="anchor" href="#%e8%af%bb-health_generate%e6%8e%a2%e6%b4%bb%e8%af%b7%e6%b1%82%e6%80%8e%e6%a0%b7%e7%a9%bf%e8%bf%87-runtime"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的章节已经分别从两个角度讲过 health check：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;request lifecycle 里，它被归为“非生成请求、探测与控制请求分叉”的一部分&lt;/li&gt;
&lt;li&gt;extension/debugging 里，它被归为 liveness 链的一部分&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;但如果你真的要顺着源码读进去，仍然会碰到一个具体问题：&lt;/p&gt;
&lt;p&gt;&lt;code&gt;/health&lt;/code&gt; / &lt;code&gt;/health_generate&lt;/code&gt; 这类请求，究竟是普通生成请求的缩小版，还是一条被 runtime 明确特判的特殊路径？&lt;/p&gt;
&lt;p&gt;这章的目标，就是把这条探活源码链单独讲清。&lt;/p&gt;
&lt;h2 id="为什么这条链值得单独成章"&gt;为什么这条链值得单独成章&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e6%9d%a1%e9%93%be%e5%80%bc%e5%be%97%e5%8d%95%e7%8b%ac%e6%88%90%e7%ab%a0"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;如果不把它单独讲出来，读者很容易形成两种都不准确的理解：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;以为 health check 就是随便发一条普通请求看看能不能回&lt;/li&gt;
&lt;li&gt;以为 health check 只是 HTTP server 自己返回个 200，不真正经过 runtime&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;源码实际走的是第三种更有工程意味的路径：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;健康检查确实会构造真实请求对象进入 runtime&lt;/li&gt;
&lt;li&gt;但 scheduler 又会在 busy 场景下对它做特殊处理，避免它被长 prefill 阻塞&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这说明它是一条“部分真实、部分特判”的探活路径。这样的设计非常值得一本技术书专门解释。&lt;/p&gt;
&lt;h2 id="一张图health-check-不是单纯-http-200也不是完全普通请求"&gt;一张图：health check 不是单纯 HTTP 200，也不是完全普通请求&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%behealth-check-%e4%b8%8d%e6%98%af%e5%8d%95%e7%ba%af-http-200%e4%b9%9f%e4%b8%8d%e6%98%af%e5%ae%8c%e5%85%a8%e6%99%ae%e9%80%9a%e8%af%b7%e6%b1%82"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会把探活链路想成普通生成请求的微缩版，但 scheduler 中间 actually 有专门的 shortcut。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 HC[&amp;#34;/health or /health_generate&amp;#34;] --&amp;gt; TM[&amp;#34;TokenizerManager.generate_request(...)&amp;#34;]
 TM --&amp;gt; SCH[&amp;#34;Scheduler.process_input_requests(...)&amp;#34;]
 SCH --&amp;gt;|busy| Sig[&amp;#34;return_health_check_ipcs + HealthCheckOutput&amp;#34;]
 SCH --&amp;gt;|idle| Normal[&amp;#34;normal request dispatch&amp;#34;]
 Sig --&amp;gt; TM2[&amp;#34;TokenizerManager.last_receive_tstamp&amp;#34;]
 Normal --&amp;gt; DET[&amp;#34;Detokenizer / normal return path&amp;#34;]
 DET --&amp;gt; TM2&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：health check 同时具备两种人格。&lt;/p&gt;</description></item><item><title>7.27 读 BasePrefixCache 与 radix_cache.py</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-prefix-cache-tree/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-prefix-cache-tree/</guid><description>&lt;h1 id="读-baseprefixcache-与-radix_cachepy"&gt;读 BasePrefixCache 与 radix_cache.py&lt;a class="anchor" href="#%e8%af%bb-baseprefixcache-%e4%b8%8e-radix_cachepy"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的调度与内存章节已经把 prefix match、&lt;code&gt;cache_protected_len&lt;/code&gt;、host hit 和 load-back 讲得比较深了，但如果你真正打开源码，仍然会遇到一个很具体的问题：&lt;/p&gt;
&lt;p&gt;prefix cache 这条主线到底该从哪两个文件开始读，怎样把“抽象 trait”和“真正的 radix tree 实现”接起来？&lt;/p&gt;
&lt;p&gt;这条线最核心的两个入口其实是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;base_prefix_cache.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;radix_cache.py&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这章的目标，就是把这两棵树收成一条稳定阅读路径。&lt;/p&gt;
&lt;h2 id="为什么这两棵树必须配对读"&gt;为什么这两棵树必须配对读&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e4%b8%a4%e6%a3%b5%e6%a0%91%e5%bf%85%e9%a1%bb%e9%85%8d%e5%af%b9%e8%af%bb"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;单读 &lt;code&gt;base_prefix_cache.py&lt;/code&gt;，你会看到：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;MatchPrefixParams&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;MatchResult&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;InsertParams&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;EvictParams&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;inc_lock_ref(...)&lt;/code&gt; / &lt;code&gt;dec_lock_ref(...)&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;但你还不知道这些抽象在真实树结构里怎样落地。&lt;/p&gt;
&lt;p&gt;单读 &lt;code&gt;radix_cache.py&lt;/code&gt;，你会看到：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;TreeNode&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;RadixKey&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;match_prefix(...)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;_match_prefix_helper(...)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;cache_unfinished_req(...)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;cache_finished_req(...)&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;但你又容易在具体实现细节里迷路，看不清哪些是“prefix cache 家族通用接口”，哪些才是 radix tree 的特化行为。&lt;/p&gt;
&lt;p&gt;所以更稳的方式是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;先用 &lt;code&gt;BasePrefixCache&lt;/code&gt; 建立抽象边界&lt;/li&gt;
&lt;li&gt;再用 &lt;code&gt;RadixCache&lt;/code&gt; 看这套抽象怎样真正落地&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="一张图prefix-cache-这棵树为什么分成抽象边界和具体实现两层"&gt;一张图：prefix cache 这棵树为什么分成“抽象边界”和“具体实现”两层&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%beprefix-cache-%e8%bf%99%e6%a3%b5%e6%a0%91%e4%b8%ba%e4%bb%80%e4%b9%88%e5%88%86%e6%88%90%e6%8a%bd%e8%b1%a1%e8%be%b9%e7%95%8c%e5%92%8c%e5%85%b7%e4%bd%93%e5%ae%9e%e7%8e%b0%e4%b8%a4%e5%b1%82"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会直接钻进 &lt;code&gt;RadixCache.match_prefix(...)&lt;/code&gt;，结果一开始就被树节点和 split 细节淹没。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart TD
 Base[&amp;#34;BasePrefixCache\ntrait + params + results&amp;#34;] --&amp;gt; Ops[&amp;#34;match / insert / evict / lock&amp;#34;]
 Ops --&amp;gt; Radix[&amp;#34;RadixCache&amp;#34;]
 Radix --&amp;gt; Node[&amp;#34;TreeNode / RadixKey&amp;#34;]
 Radix --&amp;gt; Reuse[&amp;#34;cache_unfinished_req / cache_finished_req&amp;#34;]
 Radix --&amp;gt; Evict[&amp;#34;evictable / protected / eviction policy&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：&lt;code&gt;radix_cache.py&lt;/code&gt; 不是孤立大文件，它是在实现 &lt;code&gt;BasePrefixCache&lt;/code&gt; 这一层统一语义。&lt;/p&gt;</description></item><item><title>7.28 读运行时控制面：logging、weights group 与 expert record</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-runtime-control-surfaces/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-runtime-control-surfaces/</guid><description>&lt;h1 id="读运行时控制面loggingweights-group-与-expert-record"&gt;读运行时控制面：logging、weights group 与 expert record&lt;a class="anchor" href="#%e8%af%bb%e8%bf%90%e8%a1%8c%e6%97%b6%e6%8e%a7%e5%88%b6%e9%9d%a2loggingweights-group-%e4%b8%8e-expert-record"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的代码导读已经把：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;请求主线&lt;/li&gt;
&lt;li&gt;输出尾部&lt;/li&gt;
&lt;li&gt;prefix cache&lt;/li&gt;
&lt;li&gt;多 worker&lt;/li&gt;
&lt;li&gt;session&lt;/li&gt;
&lt;li&gt;health check&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这些主要脊梁都落回了源码，但还有一类对维护者非常重要、却仍然散落在 &lt;code&gt;http_server.py&lt;/code&gt;、&lt;code&gt;tokenizer_communicator_mixin.py&lt;/code&gt;、&lt;code&gt;scheduler.py&lt;/code&gt;、&lt;code&gt;engine.py&lt;/code&gt; 与若干 util 文件里的控制面没有被正式收束：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;configure_logging&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;init_weights_update_group&lt;/code&gt; / &lt;code&gt;destroy_weights_update_group&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;start_expert_distribution_record&lt;/code&gt; / &lt;code&gt;dump_expert_distribution_record&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;remote_instance_transfer_engine_info&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这些不是普通业务 API，也不是最底层 debug hack，而是运行时真正对外暴露的维护和协调动作。&lt;/p&gt;
&lt;p&gt;这章的目标，就是把这组运行时控制面收成一条可读的源码路径。&lt;/p&gt;
&lt;h2 id="为什么这层值得单独成章"&gt;为什么这层值得单独成章&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e5%b1%82%e5%80%bc%e5%be%97%e5%8d%95%e7%8b%ac%e6%88%90%e7%ab%a0"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;因为技术书如果只讲：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;请求怎样跑&lt;/li&gt;
&lt;li&gt;模型怎样执行&lt;/li&gt;
&lt;li&gt;指标怎样看&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;却不讲“维护者怎样通过正式控制面改变系统行为”，整本书最终还是会偏向架构概览，而不是能带去现场用的系统书。&lt;/p&gt;
&lt;p&gt;这些控制面共同回答的是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;你怎样切换请求日志与 dump 行为&lt;/li&gt;
&lt;li&gt;你怎样初始化远端权重更新组&lt;/li&gt;
&lt;li&gt;你怎样打开 / 关闭 expert distribution 记录&lt;/li&gt;
&lt;li&gt;你怎样查询远端 transfer 协调信息&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;也就是说，它们构成了“系统运行中怎样被控制和协同”的另一条脊梁。&lt;/p&gt;
&lt;h2 id="一张图这组控制面不是平铺端点而是三类维护动作"&gt;一张图：这组控制面不是平铺端点，而是三类维护动作&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%be%e8%bf%99%e7%bb%84%e6%8e%a7%e5%88%b6%e9%9d%a2%e4%b8%8d%e6%98%af%e5%b9%b3%e9%93%ba%e7%ab%af%e7%82%b9%e8%80%8c%e6%98%af%e4%b8%89%e7%b1%bb%e7%bb%b4%e6%8a%a4%e5%8a%a8%e4%bd%9c"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会把这些 API 看成一串零散运维端点，但它们实际上可以分成三个稳定簇。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart TD
 Log[&amp;#34;日志与证据控制\nconfigure_logging&amp;#34;] --&amp;gt; TM[&amp;#34;TokenizerManager&amp;#34;]
 Weights[&amp;#34;远端权重协调\nweights_update_group / transfer info&amp;#34;] --&amp;gt; Comm[&amp;#34;Communicator / tp_worker / model_runner&amp;#34;]
 Experts[&amp;#34;专家分布记录\nstart/stop/dump expert record&amp;#34;] --&amp;gt; Rec[&amp;#34;scheduler / expert_distribution recorder&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：这组控制面不是同一层级的“杂项端点”，而是三类不同的维护动作。&lt;/p&gt;</description></item><item><title>7.29 读 request_logger 与 schedule_simulator</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-request-logger-and-schedule-simulator/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-request-logger-and-schedule-simulator/</guid><description>&lt;h1 id="读-request_logger-与-schedule_simulator"&gt;读 request_logger 与 schedule_simulator&lt;a class="anchor" href="#%e8%af%bb-request_logger-%e4%b8%8e-schedule_simulator"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的代码导读已经把请求主线、cache 树、输出尾部和运行时控制面都落回了源码，但还有一条很有工程价值、同时也很像“大书后半程才会出现”的闭环没有被放进代码导读：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;线上请求日志怎样真正落盘&lt;/li&gt;
&lt;li&gt;这些日志怎样再被离线调度模拟器消费&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;也就是说，如果你已经抓到了线上 request 日志，源码里下一步该去哪里读，才能把它变成调度策略实验材料？&lt;/p&gt;
&lt;p&gt;这章的目标，就是把：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;srt/utils/request_logger.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;TokenizerManager.configure_logging(...)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;debug_utils/schedule_simulator/&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;收成一条正式的源码阅读路径。&lt;/p&gt;
&lt;h2 id="为什么这条链值得放进代码导读"&gt;为什么这条链值得放进代码导读&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e6%9d%a1%e9%93%be%e5%80%bc%e5%be%97%e6%94%be%e8%bf%9b%e4%bb%a3%e7%a0%81%e5%af%bc%e8%af%bb"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;因为它不是纯维护技巧，也不是纯工具手册，而是一条真实的源码工作流：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;运行时怎样决定日志口径&lt;/li&gt;
&lt;li&gt;日志里哪些字段会被保留下来&lt;/li&gt;
&lt;li&gt;这些字段怎样被 loader 缩成模拟请求&lt;/li&gt;
&lt;li&gt;模拟器又怎样把它们送进 router / scheduler policy&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;从技术书角度看，这条链特别有价值，因为它说明：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;线上证据并不是终点&lt;/li&gt;
&lt;li&gt;还可以继续变成离线实验输入&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="一张图这不是两件工具而是一条连续链"&gt;一张图：这不是两件工具，而是一条连续链&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%be%e8%bf%99%e4%b8%8d%e6%98%af%e4%b8%a4%e4%bb%b6%e5%b7%a5%e5%85%b7%e8%80%8c%e6%98%af%e4%b8%80%e6%9d%a1%e8%bf%9e%e7%bb%ad%e9%93%be"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会把 request logger 和 simulator 看成两套完全分开的工具，实际上它们在源码里是可以接起来的。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 Req[&amp;#34;runtime request&amp;#34;] --&amp;gt; Log[&amp;#34;RequestLogger.log_finished_request(...)&amp;#34;]
 Log --&amp;gt; File[&amp;#34;request.finished JSON lines&amp;#34;]
 File --&amp;gt; Loader[&amp;#34;load_from_request_logger(...)&amp;#34;]
 Loader --&amp;gt; Sim[&amp;#34;schedule_simulator&amp;#34;]
 Sim --&amp;gt; Policy[&amp;#34;router / scheduler policy replay&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：日志不是只给人看，也可以成为策略实验的输入格式。&lt;/p&gt;
&lt;h2 id="第一棵树requestlogger-应该怎么读"&gt;第一棵树：&lt;code&gt;RequestLogger&lt;/code&gt; 应该怎么读&lt;a class="anchor" href="#%e7%ac%ac%e4%b8%80%e6%a3%b5%e6%a0%91requestlogger-%e5%ba%94%e8%af%a5%e6%80%8e%e4%b9%88%e8%af%bb"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;如果要稳，最先抓的其实只有三处：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;configure(...)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;log_received_request(...)&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;log_finished_request(...)&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;这样读的好处是，你会立刻看清：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;运行中日志行为是否可变&lt;/li&gt;
&lt;li&gt;首尾日志分别保留了什么&lt;/li&gt;
&lt;li&gt;finished request 为什么会成为后续模拟的样本源&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="metadata-为什么是-requestlogger-的真正核心"&gt;&lt;code&gt;metadata&lt;/code&gt; 为什么是 &lt;code&gt;RequestLogger&lt;/code&gt; 的真正核心&lt;a class="anchor" href="#metadata-%e4%b8%ba%e4%bb%80%e4%b9%88%e6%98%af-requestlogger-%e7%9a%84%e7%9c%9f%e6%ad%a3%e6%a0%b8%e5%bf%83"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;RequestLogger&lt;/code&gt; 初始化时会立刻算：&lt;/p&gt;</description></item><item><title>7.30 读 TemplateManager 与请求渲染链</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-template-manager-and-request-rendering/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-template-manager-and-request-rendering/</guid><description>&lt;h1 id="读-templatemanager-与请求渲染链"&gt;读 TemplateManager 与请求渲染链&lt;a class="anchor" href="#%e8%af%bb-templatemanager-%e4%b8%8e%e8%af%b7%e6%b1%82%e6%b8%b2%e6%9f%93%e9%93%be"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的运行时架构已经在 &lt;a href="../runtime-architecture/template-manager-and-request-rendering/"&gt;3.6 TemplateManager、chat template 与请求解释边界&lt;/a&gt; 里解释了模板层为什么重要，但如果你真正要顺着源码读进去，仍然会遇到一个更具体的问题：&lt;/p&gt;
&lt;p&gt;这条“请求解释层”到底该从哪些文件开始读，怎样把：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;TemplateManager&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;chat / completion template&lt;/li&gt;
&lt;li&gt;serving handler&lt;/li&gt;
&lt;li&gt;&lt;code&gt;GenerateReqInput&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;连成一条稳定源码路径？&lt;/p&gt;
&lt;p&gt;这章的目标，就是把这条请求渲染链正式收成代码导读的一章。&lt;/p&gt;
&lt;h2 id="为什么这条链值得放进代码导读"&gt;为什么这条链值得放进代码导读&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e6%9d%a1%e9%93%be%e5%80%bc%e5%be%97%e6%94%be%e8%bf%9b%e4%bb%a3%e7%a0%81%e5%af%bc%e8%af%bb"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;因为它正好站在一个很微妙、又非常重要的位置：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;它不是纯 API 协议层&lt;/li&gt;
&lt;li&gt;也不是 scheduler / execution 那种后半段 runtime 逻辑&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;它回答的问题更像：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;用户给了一串 messages / prompt / suffix&lt;/li&gt;
&lt;li&gt;系统到底怎样把它们解释成真正会进入 tokenizer 和 runtime 的东西&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这类问题如果没有一章正式导读，读者很容易在：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;serving_chat.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;template_manager.py&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;tokenizer 的 &lt;code&gt;apply_chat_template(...)&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;之间来回跳，却始终不清楚真正的解释边界在哪里。&lt;/p&gt;
&lt;h2 id="一张图请求渲染链比协议转对象多了一层语义解释"&gt;一张图：请求渲染链比“协议转对象”多了一层语义解释&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%be%e8%af%b7%e6%b1%82%e6%b8%b2%e6%9f%93%e9%93%be%e6%af%94%e5%8d%8f%e8%ae%ae%e8%bd%ac%e5%af%b9%e8%b1%a1%e5%a4%9a%e4%ba%86%e4%b8%80%e5%b1%82%e8%af%ad%e4%b9%89%e8%a7%a3%e9%87%8a"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会把请求渲染误解成“协议对象转内部对象”的同义词，但中间其实还隔着模板与内容格式解释层。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 Req[&amp;#34;messages / prompt / suffix&amp;#34;] --&amp;gt; Handler[&amp;#34;serving_chat / serving_completions&amp;#34;]
 Handler --&amp;gt; Tpl[&amp;#34;TemplateManager&amp;#34;]
 Tpl --&amp;gt; Render[&amp;#34;apply_chat_template / completion template&amp;#34;]
 Render --&amp;gt; Obj[&amp;#34;GenerateReqInput&amp;#34;]
 Obj --&amp;gt; TM[&amp;#34;TokenizerManager.generate_request()&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：handler 不只是简单把协议字段塞进对象，而是会先经过一层“请求解释”。&lt;/p&gt;</description></item><item><title>7.31 读 model_runner.py：执行壳的真正汇合点</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-model-runner-core/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-model-runner-core/</guid><description>&lt;h1 id="读-model_runnerpy执行壳的真正汇合点"&gt;读 model_runner.py：执行壳的真正汇合点&lt;a class="anchor" href="#%e8%af%bb-model_runnerpy%e6%89%a7%e8%a1%8c%e5%a3%b3%e7%9a%84%e7%9c%9f%e6%ad%a3%e6%b1%87%e5%90%88%e7%82%b9"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的运行时架构和执行模型已经反复把 &lt;code&gt;ModelRunner&lt;/code&gt; 当成核心汇合点来讲，但代码导读里仍然缺一章真正面对这棵源码树本身：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;python/sglang/srt/model_executor/model_runner.py&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果你真的打开它，会很容易被几类逻辑同时淹没：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;模型加载&lt;/li&gt;
&lt;li&gt;KV cache 初始化&lt;/li&gt;
&lt;li&gt;attention backend 初始化&lt;/li&gt;
&lt;li&gt;distributed / MoE / expert recorder&lt;/li&gt;
&lt;li&gt;&lt;code&gt;forward()&lt;/code&gt; / &lt;code&gt;sample()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;hidden states 与 routed experts 旁路返回&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果没有一章专门讲“这棵树应该怎么读”，读者很容易只记住它是个大文件，而不是整本书真正最关键的执行壳。&lt;/p&gt;
&lt;p&gt;这章的目标，就是把 &lt;code&gt;model_runner.py&lt;/code&gt; 压成一条稳定的源码阅读骨架。&lt;/p&gt;
&lt;h2 id="为什么这棵树值得单独成章"&gt;为什么这棵树值得单独成章&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e6%a3%b5%e6%a0%91%e5%80%bc%e5%be%97%e5%8d%95%e7%8b%ac%e6%88%90%e7%ab%a0"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;因为它正好站在全书多条主线的交叉口：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;运行时架构里，它是 worker 壳之内真正的执行汇合点&lt;/li&gt;
&lt;li&gt;调度与内存里，它要把 batch 视图落成可执行状态&lt;/li&gt;
&lt;li&gt;执行模型里，&lt;code&gt;forward()&lt;/code&gt; / &lt;code&gt;sample()&lt;/code&gt; 的真正入口就在这里&lt;/li&gt;
&lt;li&gt;旁路返回语义里，hidden states / routed experts / MoE recorder 也都在这里汇合&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;也就是说，这不是“又一个重要文件”，而是整本书里最接近“单文件缩影”的源码节点。&lt;/p&gt;
&lt;h2 id="一张图modelrunner-为什么是执行壳而不是单纯-forward-封装"&gt;一张图：&lt;code&gt;ModelRunner&lt;/code&gt; 为什么是执行壳而不是单纯 forward 封装&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%bemodelrunner-%e4%b8%ba%e4%bb%80%e4%b9%88%e6%98%af%e6%89%a7%e8%a1%8c%e5%a3%b3%e8%80%8c%e4%b8%8d%e6%98%af%e5%8d%95%e7%ba%af-forward-%e5%b0%81%e8%a3%85"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会把 &lt;code&gt;ModelRunner&lt;/code&gt; 想成“包一层 &lt;code&gt;model.forward()&lt;/code&gt;”，但源码实际上让它承担了更广的汇合职责。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart TD
 Load[&amp;#34;load_model()&amp;#34;] --&amp;gt; KV[&amp;#34;init_memory_pool()&amp;#34;]
 KV --&amp;gt; Attn[&amp;#34;init_attention_backend()&amp;#34;]
 Attn --&amp;gt; Fwd[&amp;#34;forward(forward_batch)&amp;#34;]
 Fwd --&amp;gt; Sample[&amp;#34;sample(logits_output, forward_batch)&amp;#34;]
 Fwd --&amp;gt; Side[&amp;#34;hidden states / routed experts / expert recorder&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：&lt;code&gt;ModelRunner&lt;/code&gt; 不是只站在执行后半段，而是从模型、缓存、后端、到输出语义都在同一处汇流。&lt;/p&gt;</description></item><item><title>7.32 读 model_loader：权重如何进入执行壳</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-model-loader-and-weight-loading/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-model-loader-and-weight-loading/</guid><description>&lt;h1 id="读-model_loader权重如何进入执行壳"&gt;读 model_loader：权重如何进入执行壳&lt;a class="anchor" href="#%e8%af%bb-model_loader%e6%9d%83%e9%87%8d%e5%a6%82%e4%bd%95%e8%bf%9b%e5%85%a5%e6%89%a7%e8%a1%8c%e5%a3%b3"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的运行时架构已经讲了：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;模型定义和执行壳的边界&lt;/li&gt;
&lt;li&gt;远端 transfer 协调&lt;/li&gt;
&lt;li&gt;热更新控制面&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;但如果你真正想在源码里回答“模型权重到底是怎样被装进执行壳”的问题，仍然还缺一个正式入口：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;model_runner.py::load_model()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;model_loader/loader.py&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这条链非常关键，因为很多运行时问题其实都发生在这里：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;load format 选择不对&lt;/li&gt;
&lt;li&gt;远端实例权重加载没走通&lt;/li&gt;
&lt;li&gt;默认 loader 和量化 loader 的行为差异&lt;/li&gt;
&lt;li&gt;权重热更新为什么会回到 &lt;code&gt;DefaultModelLoader&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这章的目标，就是把“权重如何进入执行壳”收成一条稳定的源码阅读路径。&lt;/p&gt;
&lt;h2 id="为什么这条链值得单独成章"&gt;为什么这条链值得单独成章&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e6%9d%a1%e9%93%be%e5%80%bc%e5%be%97%e5%8d%95%e7%8b%ac%e6%88%90%e7%ab%a0"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;因为如果只看 &lt;code&gt;ModelRunner&lt;/code&gt;，你会知道它会 &lt;code&gt;load_model()&lt;/code&gt;；如果只看控制面，你会知道它能 &lt;code&gt;update_weights_from_*&lt;/code&gt;。但两者中间真正承接“怎么加载、从哪加载、用什么 loader 加载”的系统，却在：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;model_loader/loader.py&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;里。&lt;/p&gt;
&lt;p&gt;这意味着：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;权重加载既不是外围工具&lt;/li&gt;
&lt;li&gt;也不是 &lt;code&gt;ModelRunner&lt;/code&gt; 自己的局部细节&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;它是执行壳能否真正成立的地基之一。&lt;/p&gt;
&lt;h2 id="一张图权重不是直接读盘给模型而是经过一条-loader-链"&gt;一张图：权重不是直接读盘给模型，而是经过一条 loader 链&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%be%e6%9d%83%e9%87%8d%e4%b8%8d%e6%98%af%e7%9b%b4%e6%8e%a5%e8%af%bb%e7%9b%98%e7%bb%99%e6%a8%a1%e5%9e%8b%e8%80%8c%e6%98%af%e7%bb%8f%e8%bf%87%e4%b8%80%e6%9d%a1-loader-%e9%93%be"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者会默认 &lt;code&gt;ModelRunner.load_model()&lt;/code&gt; 里就是几行读权重代码，但实际系统先编译 &lt;code&gt;LoadConfig&lt;/code&gt;，再选 loader，再进具体加载路径。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 Args[&amp;#34;ServerArgs / LoadFormat&amp;#34;] --&amp;gt; Config[&amp;#34;LoadConfig&amp;#34;]
 Config --&amp;gt; Select[&amp;#34;get_model_loader(...)&amp;#34;]
 Select --&amp;gt; Loader[&amp;#34;Default / Remote / Quantized / Layered ...&amp;#34;]
 Loader --&amp;gt; Model[&amp;#34;loader.load_model(...)&amp;#34;]
 Model --&amp;gt; Runner[&amp;#34;ModelRunner execution shell&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：权重加载本身就是一条被显式建模的可插拔链路，而不是 &lt;code&gt;ModelRunner&lt;/code&gt; 内部不可见的一步。&lt;/p&gt;</description></item><item><title>7.33 读 forward_batch_info.py：执行对象如何真正落地</title><link>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-forward-batch-info/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kingye.me/study-ml/docs/book/sglang/codebase-walkthrough/reading-forward-batch-info/</guid><description>&lt;h1 id="读-forward_batch_infopy执行对象如何真正落地"&gt;读 forward_batch_info.py：执行对象如何真正落地&lt;a class="anchor" href="#%e8%af%bb-forward_batch_infopy%e6%89%a7%e8%a1%8c%e5%af%b9%e8%b1%a1%e5%a6%82%e4%bd%95%e7%9c%9f%e6%ad%a3%e8%90%bd%e5%9c%b0"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="这章解决什么问题"&gt;这章解决什么问题&lt;a class="anchor" href="#%e8%bf%99%e7%ab%a0%e8%a7%a3%e5%86%b3%e4%bb%80%e4%b9%88%e9%97%ae%e9%a2%98"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;前面的运行时架构和执行模型已经反复把：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;ScheduleBatch&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ModelWorkerBatch&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ForwardBatch&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;当成理解执行主线的关键对象，但代码导读里仍然缺一章真正面对：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;python/sglang/srt/model_executor/forward_batch_info.py&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这棵树本身。&lt;/p&gt;
&lt;p&gt;如果你真的打开这个文件，会发现它不只是一个 dataclass 集合，而是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;前向执行低层对象&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ForwardMode&lt;/code&gt; 语义中心&lt;/li&gt;
&lt;li&gt;positions、extend、spec_info、LoRA、DP attention、mrope、多模态、split prefill&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这些执行主线变量真正落地的汇合点。&lt;/p&gt;
&lt;p&gt;这章的目标，就是把 &lt;code&gt;forward_batch_info.py&lt;/code&gt; 收成一条可读的源码骨架。&lt;/p&gt;
&lt;h2 id="为什么这棵树值得单独成章"&gt;为什么这棵树值得单独成章&lt;a class="anchor" href="#%e4%b8%ba%e4%bb%80%e4%b9%88%e8%bf%99%e6%a3%b5%e6%a0%91%e5%80%bc%e5%be%97%e5%8d%95%e7%8b%ac%e6%88%90%e7%ab%a0"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;因为它正好位于整本书几条主线的真正接缝上：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;调度与内存里，&lt;code&gt;ScheduleBatch&lt;/code&gt; 在这里降成执行视图&lt;/li&gt;
&lt;li&gt;运行时架构里，&lt;code&gt;ModelRunner&lt;/code&gt; 正式消费的就是它&lt;/li&gt;
&lt;li&gt;执行模型里，很多行为分支都先看 &lt;code&gt;ForwardMode&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;也就是说，如果 &lt;code&gt;ModelRunner&lt;/code&gt; 是执行壳的汇合点，那 &lt;code&gt;ForwardBatch&lt;/code&gt; 就是执行壳真正吃下去的对象形状。&lt;/p&gt;
&lt;p&gt;技术书如果不把这层单独讲出来，读者会在很多章节里反复看到它，却没有一个稳定的源码回落点。&lt;/p&gt;
&lt;h2 id="一张图这棵树为什么是调度视图和执行视图之间的最后一层桥"&gt;一张图：这棵树为什么是调度视图和执行视图之间的最后一层桥&lt;a class="anchor" href="#%e4%b8%80%e5%bc%a0%e5%9b%be%e8%bf%99%e6%a3%b5%e6%a0%91%e4%b8%ba%e4%bb%80%e4%b9%88%e6%98%af%e8%b0%83%e5%ba%a6%e8%a7%86%e5%9b%be%e5%92%8c%e6%89%a7%e8%a1%8c%e8%a7%86%e5%9b%be%e4%b9%8b%e9%97%b4%e7%9a%84%e6%9c%80%e5%90%8e%e4%b8%80%e5%b1%82%e6%a1%a5"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这张图解决的理解障碍是：很多读者知道有 &lt;code&gt;ForwardBatch&lt;/code&gt;，但并不真正清楚它和 &lt;code&gt;ScheduleBatch&lt;/code&gt; / &lt;code&gt;ModelWorkerBatch&lt;/code&gt; 的转换关系。&lt;/p&gt;
&lt;pre class="mermaid"&gt;flowchart LR
 S[&amp;#34;ScheduleBatch&amp;#34;] --&amp;gt; M[&amp;#34;ModelWorkerBatch&amp;#34;]
 M --&amp;gt; F[&amp;#34;ForwardBatch.init_new(...)&amp;#34;]
 F --&amp;gt; R[&amp;#34;ModelRunner.forward()&amp;#34;]
 F --&amp;gt; Modes[&amp;#34;ForwardMode / CaptureHiddenMode / spec_info&amp;#34;]&lt;/pre&gt;&lt;p&gt;图比纯文字多解释的一点是：这棵树不是执行层里的独立小对象，而是调度与执行交界处真正的翻译层。&lt;/p&gt;
&lt;h2 id="第一层为什么文件头注释本身就值得认真读"&gt;第一层：为什么文件头注释本身就值得认真读&lt;a class="anchor" href="#%e7%ac%ac%e4%b8%80%e5%b1%82%e4%b8%ba%e4%bb%80%e4%b9%88%e6%96%87%e4%bb%b6%e5%a4%b4%e6%b3%a8%e9%87%8a%e6%9c%ac%e8%ba%ab%e5%b0%b1%e5%80%bc%e5%be%97%e8%ae%a4%e7%9c%9f%e8%af%bb"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;这个文件一开头就把最重要的事写出来了：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;ScheduleBatch -&amp;gt; ModelWorkerBatch -&amp;gt; ForwardBatch&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;并且还明确说明：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;ScheduleBatch&lt;/code&gt; 更偏 scheduler 侧的高层 CPU 视图&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ModelWorkerBatch&lt;/code&gt; 是给 worker 的 GPU 相关子集&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ForwardBatch&lt;/code&gt; 则是 &lt;code&gt;ModelRunner&lt;/code&gt; 真正消费的低层 tensor 视图&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这意味着优秀的阅读策略不是“把这个文件当作实现细节”，而是先把它当作：&lt;/p&gt;</description></item></channel></rss>